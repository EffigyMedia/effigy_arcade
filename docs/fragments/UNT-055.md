+++
id                = "UNT-055"
kind              = "unit"
status            = "closed"
cost              = 2277
closed            = "2026-08-30T09:49:53-04:00"
created           = "2026-08-30T09:49:15-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "docs"
+++

### UNT-055 - Record why a unit landed in the environment store, and guard this project against it

- Routing class: `docs` -> **claude-sonnet-5**, effort **medium**. Bias 1. Writing to a settled decision. the judgment happened when the decision did. ([[RLG-092]])
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-30T09:49:53-04:00** - The strays in the environment store are gone, on the owner's ruling to delete rather than move: the project store already held the same three passing runs as UNT-051, so a move would have put two units in one store for one piece of work. The environment's store is byte-identical to its last commit, its thread was restored, all 711 of its fragments validate and nothing referenced the removed ids. RLG-083 records the cause, which is not carelessness: an omitted --store means the environment's store in every fragment tool, from every working directory, while jurisdiction.home() - the function that resolves it correctly, whose own docstring describes this exact failure - is called by no CLI path. This project now passes --store on every call as a thread constraint. The fix to the shared tooling is the environment's to make and is written up in the note at its root.
