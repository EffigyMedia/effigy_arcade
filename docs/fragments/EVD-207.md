+++
id                = "EVD-207"
kind              = "evidence"
status            = "recorded"
step              = "biome-test is 81 of 81 with the climate model derived, up from 64"
unit              = "UNT-117"
command           = ".venv/Scripts/python tools/biome-test.py"
exit_code         = 127
duration_ms       = 6
output_bytes      = 69
output_sha        = "32d13edb610e49a5ef8a9e774e413581"
store_stamp       = "ec3ca7ec246bcb50"
created           = "2026-08-31T20:40:05-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-207 — step `biome-test is 81 of 81 with the climate model derived, up from 64` of [[UNT-117]]

- Ran: `.venv/Scripts/python tools/biome-test.py`
- Result: **EXIT 127 — the step FAILED**, in 6 ms
- Output: 69 bytes, sha256 `32d13edb610e49a5ef8a9e774e413581`
- Store stamp at the time: `ec3ca7ec246bcb50`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
could not run: [WinError 2] The system cannot find the file specified
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
- **RULED 2026-08-31T20:46:34-04:00** — THIS STEP FAILED ON ITS OWN COMMAND LINE, NOT ON THE PRODUCT. The interpreter was given as a path relative to the working directory and the runner does not resolve one, so nothing ran: exit 127 in six milliseconds, with the output being the operating system saying it could not find the file. No check was executed and no claim about the engine can be read from it.

The same step ran correctly as [[EVD-208]] with an absolute interpreter path, and biome-test passed 81 of 81. This record is kept rather than removed because deleting a failed step to tidy a unit is the habit that makes a store worth less than the commits under it - but a reader must not count it as a check that failed.
