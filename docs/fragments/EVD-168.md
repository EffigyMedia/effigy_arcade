+++
id                = "EVD-168"
kind              = "evidence"
status            = "recorded"
step              = "the chase gets equally far in equal time at 60fps and at 17fps, and counting frames again makes it go red"
unit              = "UNT-093"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/skychase-test.py"
exit_code         = 0
duration_ms       = 15398
output_bytes      = 949
output_sha        = "ea3fa59ef747bb5205403e81638edd3d"
store_stamp       = "f65840fcb5d78c4f"
created           = "2026-08-31T08:00:59-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-168 — step `the chase gets equally far in equal time at 60fps and at 17fps, and counting frames again makes it go red` of [[UNT-093]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/skychase-test.py`
- Result: **exit 0**, in 15398 ms
- Output: 949 bytes, sha256 `ea3fa59ef747bb5205403e81638edd3d`
- Store stamp at the time: `f65840fcb5d78c4f`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
skychase-test  .  the skyline answers a corner in seconds, not in frames
  browser: playwright bundled chromium (pinned)
  full speed:   60.7 fps over 91 frames, 1.4343 left of 100
  throttled:    16.7 fps over 25 frames, 1.2233 left of 100

  ok    the throttle actually cut the frame rate, so two rates were compared
        the slow run ran 3.64x slower
  ok    and it kept drawing, so the chase had frames to run in
  ok    and it stayed above the step clamp, so the chase was measured and not the clamp
        the clamp sits at 8.0 fps; the slow run held 16.7
  ok    the chase gets equally far in equal TIME, whatever the frame rate
        residuals 1.4343 and 1.2233, differing by 15%

  the same check, with the chase counting frames again
  ok    counting frames makes the check go red, so it was measuring the chase
         61.3 fps -> 1.4465 left,  15.3 fps -> 34.6799 left, differing by 96%

  ok    no page errors

all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
