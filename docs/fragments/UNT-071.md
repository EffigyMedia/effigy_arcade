+++
id                = "UNT-071"
kind              = "unit"
status            = "closed"
cost              = 17750
closed            = "2026-08-30T16:55:49-04:00"
created           = "2026-08-30T16:47:47-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
delegate          = false
effort            = "high"
model             = "claude-opus-5"
ran_on            = "claude-opus-5"
routing           = "change"
+++

### UNT-071 - Under the glass: the countdown and the wanted stars

- Routing class: `change` -> **claude-opus-5**, effort **high**. Bias 1. A policy or vocabulary change reaches every file that follows it. ([[RLG-092]])
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-30T16:55:49-04:00** - DISTANCE is gone from the top row for good - it is on the odometer - and the row is centred rather than spread, which does the whole of what was asked: the countdown centres itself alone, and with the wanted stars beside it the two become one group, clock left and stars right. Five stars because heat is already 1 to 5, shown whenever pursuit is on rather than only mid-chase, because the level is what decides how thickly the traps ahead are laid. The bottle is centred over the pads and evenly spaced, and the two gaps turned out to be in different units - the pedal box's own offset was unscaled while everything inside it scales, so 7.4 and 10.2 where both were meant to be 7.4. Measured 7.4 and 7.4, against 0.0 and 14.8 with the old rule back.
