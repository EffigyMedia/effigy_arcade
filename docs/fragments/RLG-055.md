+++
id                = "RLG-055"
kind              = "ruling"
type              = "feature"
status            = "built"
created           = "2026-08-28T20:20:35-04:00"
created_precision = "second"
supersedes        = ["RLG-043"]
blocks            = []
caused_by         = ["RLG-042"]
see_also          = []
+++

### RLG-055 — The full fleet stat table, rewritten and signed off by the owner
- **RULED 2026-08-28T20:21:02-04:00** — **Owner, 2026-08-28:** "we need to also rewrite and sign off on the full stat table of the whole
fleet", and "the stats do need to include brake and grip".

**This supersedes [[RLG-043]].** That fragment asked for the production and sports class to come down
a bit, with traffic normalized so the lorry was not made worse. The job is now larger in three ways
and a fragment whose title says "lower the production and sports class" would send a later reader to
the wrong scope:

1. **the whole fleet**, not one class - every entry in `BODY`, supercars, sports, police and traffic
2. **`brake` and `grip` as well as `vmax` and `pull`** - the owner has ruled them in
3. **owner sign-off** on the finished table, which makes this a proposal to be reviewed rather than
   a change to be made and reported

RLG-043's content is not discarded: its measured findings, its class rule for the police cars, and
its normalization scheme for the traffic all carry forward and are named below.

**IT IS BLOCKED ON A MEASUREMENT THAT HAS NOT BEEN DONE.** [[RLG-042]] measured straight-line
acceleration and top speed, and found the table truthful about `vmax` and the garage card optimistic
about 0-60. **Nothing has measured `brake` or `grip`.** Lowering a number without knowing whether it
was honest produces a change nobody can interpret afterwards - which is the whole lesson of RLG-042,
where the top-speed column turned out to be true and three comments around it false. So
`stat-test.py` needs a fourth experiment first: real braking distance from a fixed speed, and real
cornering, per body, against what `brake` and `grip` declare.

**What carries forward from RLG-043, unchanged:**

- **the class rule the owner gave as a standing rule:** CRUISER is comparable to the sports class,
  SUPERCRUISER to the supercars. Measured, both are on the wrong side of their rung - CRUISER at
  190mph above every sports car, SUPERCRUISER at 184 below every supercar - so seating them is part
  of the rewrite rather than a consequence of it.
- **the traffic normalization:** pull each traffic body down toward the lorry rather than by a flat
  percentage, so the fastest traffic drops most and the lorry does not move. `new = LORRY + (old -
  LORRY) * (1 - k)`.
- **the traffic half stays blocked** until traffic actually reads `BODY` ([[RLG-042]]), because a
  number nothing consults cannot be signed off as anything.

**The rule for the numbers is [[RLG-043]]'s:** mostly realistic, biased towards fun. Realism sets the
shape and the ordering; fun breaks the tie wherever realism would make the game worse; where the two
conflict and it is not obvious, the proposal names the point rather than choosing quietly.

**Three comments are wrong and are fixed HERE rather than earlier**, which is why RLG-042 left them:
the rung comment claiming MUSCLE 184 and TUNER 172, the supercar comment claiming the fastest car
sits at 200 and that anything above 1.0 is clamped, and the SUPERCRUISER note claiming its equipment
costs 4mph when the gap is 10. They describe numbers this unit changes. Rewriting them any earlier
means writing them twice.

**Sign-off means the table is proposed, not applied.** The deliverable is the full fleet in one
readable table - every body, every stat, old against new, with the reasoning for each move and the
conflicts named - for the owner to approve, amend or reject before a line of `BODY` is edited.
- **RULED 2026-08-28T20:32:57-04:00** — **A decision has arrived for this table from [[RLG-042]] part 2, and it should be taken here rather
than separately.**

Giving the rivals their own gearbox and torque replaced a flat `2850` with the player's own
`1000 * gearFactor * pull`. Measured, a rival's recovery from 40% to 90% of pace went from **3.82s to
13.5s** - because rivals had been pulling about three and a half times harder than any car the
player can buy, with no `pull` term at all. The old number was the wrong one, but the race was tuned
around it, and the rubber band's measured effect fell with it: the governor from -7.80% to -3.21%,
and a towed rival no longer reaches the ceiling the tow raises for it.

**So the fleet table now has a job it did not have when the owner asked for it:** `pull` is the stat
that decides how a race feels, and it is currently set for cars that were never using it. The
proposal must set `pull` deliberately for every rival-capable body and say what it does to the race,
not only what it does to the garage card.

**This is the right home for that decision** rather than a constant in `aiAccel`. `pull` is
published - it is on the unlock card, it is what `zeroSixty()` reads, and it is a promise to the
player. A rival that pulls harder should be a car that pulls harder. The alternative, an AI-only
multiplier, would put "AI cars are special" back into the engine under a better name, three commits
after it was measured out of it.

**It also means the sign-off is not only about numbers being plausible.** The owner is signing off
how hard the field races, and the proposal should say so plainly and give the before-and-after from
`band-test` and the `stat-test` gearbox experiment for whatever it proposes.
- **RULED 2026-08-28T20:43:52-04:00** — **Owner, 2026-08-28, two rulings that settle the question this fragment was holding.**

> "We don't need pull and I don't want pull on any card or any front facing anything. It doesn't
> need to exist. What we want is an acceleration stat."

> "Why do the NPCs need to have quicker or faster cars? Just make them race well."

**1. `pull` is replaced by an ACCELERATION stat.** The table gets `accel`; `pull` goes.

Worth noting what is already true, so the change is not overstated: `pull` is **not on any card
today**. The unlock card prints TOP SPEED, GEARBOX, REDLINE and 0-60, and road.js:4455 already
carries a heading reading `0 TO 60, NOT "PULL"`. So the front-facing half of the ruling is already
satisfied, and what remains is the internal name and the concept behind it - a torque multiplier
called `pull` becomes a stat that means acceleration, with the 0-60 figure as its public face.

**The rename is folded INTO this table rewrite rather than done first.** `BODY` is about to be
rewritten line by line under the owner's signature; renaming a column in it beforehand means editing
every entry twice and reviewing a diff that mixes a rename with a retune. Same reason the three wrong
comments were left for this unit.

**2. NPCs DO NOT GET BETTER CARS. This closes [[RLG-042]] part 2's open decision.**

That unit found rivals had been accelerating about three and a half times harder than any car the
player can buy, fixed it, and put three options to the owner: accept the slower field, raise `pull`
for rival-capable bodies here, or add an AI-only multiplier. **The owner has rejected the second and
third and taken the first.**

So this table does **not** get a set of numbers tuned to make the field competitive. Every car gets
the stat it should have on its own merits, and a rival is competitive because it DRIVES well.

**That moves the load onto [[RLG-033]], and it should be said plainly: RLG-033 is now load-bearing
rather than polish.** Its measurement is done; parts 2, 3 and 4 - racing lines and committed
overtakes, defending, and mistakes and personality - are what a rival now has instead of a faster
car. The field got slower in v0.9.8 and the answer is that it must get better.

**It is also the more honest design and the record should say why.** A rival that beats the player
because its car pulls harder is a car the player cannot buy doing something the player cannot do. A
rival that beats the player because it picked a better line is something the player can learn from,
and then beat. The first is difficulty; the second is a game.

**One consequence for the sign-off:** the proposal can now be argued purely on "is this what this
vehicle should be", without a second column of "and does this make the race close enough". That is a
much easier table to review and a much easier one to get right.
- **RULED 2026-08-29T10:18:17-04:00** — THE BLOCKING MEASUREMENT IS DONE. `stat-test.py` has a fourth experiment, and `brake` and `grip` are
no longer numbers nobody has checked.

BRAKING. From 140mph to 40mph, brake held, no throttle, in cleared air, best of two:

    VECTOR 250m   APEX 254   COMET 264   CREST 367   STALLION 374   MATADOR 377
    ROADSTER 433  TUNER 473  CRUISER 483  COUPE 550  MUSCLE 565     SALOON 605
    CAB 599       PICKUP 661 VAN 683      LORRY 997

The order is the declared order, and the spread is four to one from the formula cars to the lorry.
`brake` is honest.

CORNERING, AND NOBODY TOUCHES THE WHEEL. The engine pushes a car to the outside of a bend at
`pushK * v^2 * dt * cornerG()`, and `cornerG()` is `0.42 / grip`. So the car is driven into one bend
at a fixed 120mph with no steering input at all, the push is integrated exactly as the engine applies
it, and the drift is divided by it. That gives `cornerG` as measured. Multiplied by the declared grip
it should read 0.42 for every car, and it does:

    mean 0.420, spread 0.406 to 0.432, standard deviation 0.006, across all sixteen bodies.

`grip` is honest too.

WHAT IT TOOK TO GET A NUMBER, because two earlier versions returned a clean-looking nothing:

1. THE AUTOPILOT WAS DRIVING. The first run reported no drift at all for all sixteen cars, which
   looked like a result. `__probe.drive()` is a centre-seeker holding the arrow keys, and it was
   steering the push out as fast as the corner made it. A grip measurement with a driver in it
   measures the driver. The driver is stopped for the whole experiment now.

2. PINNING THE CAR IN THE BEND MEASURED NOTHING EITHER. Holding one world position with `jumpTo` on
   a 16ms timer rebuilds the bend cache sixty times a second, so nothing accumulates - it is a car
   being teleported to the start of a corner, not a car in one. The car drives into the bend now.

3. A RATE IS NOT COMPARABLE BETWEEN TWO CARS unless they were pushed by the same amount, and they
   never are: the entry point differs and `pushK` lags the road by CORNER_LAG. Integrating the push
   and dividing by it removes both.

4. A CAR STARTS WHERE THE LAST ONE STOPPED. The verge snaps `targetX = playerX*0.7` when a car is
   against the barrier - a third of a lane, arriving inside the measurement as though a corner had
   caused it. Two bodies read 2.0 instead of 0.42 from exactly that. `setLane(0)` between cars.

THE CHECK WAS WATCHED FAILING. With `grip` removed from `cornerG` and `brake` removed from the brake
rate, the same run reports every car stopping in 483m and `x grip` spreading from 0.175 to 0.871
(sd 0.212 against 0.006). Both halves detect their own defect, so neither column is vacuous.

THREE NEW INSTRUMENTS, all read-only except the two that a harness cannot do without: `setBrake`
(the same call the brake button makes, not a shortcut past it), `pushK`, `targetX`, `curvatureAt`,
and `setLane`.

The table proposal can now be written against measured behaviour rather than against declarations.
- **RULED 2026-08-29T10:39:46-04:00** — THE PROPOSAL. The whole fleet, old against new, for the owner to approve, amend or reject. NOTHING IS
APPLIED - `BODY` is untouched until this is signed off, which is what this fragment asked for.

Every number below is measured rather than assumed: 0-60 and top speed from experiment 1, braking
distance and cornering from experiment 4, and the proposed 0-60 figures are run through the same
simulation the garage card prints, so they are the numbers a player will actually read.

WHAT IS WRONG TODAY, from the measurements rather than from opinion:

  1. THE LADDER OVERLAPS ITSELF. The sports class is the starting class since RLG-070, and its
     ceiling - MUSCLE at 188mph - sits 6mph under the slowest supercar. A class you graduate into
     should be a step rather than a nudge.
  2. THE CRUISER OUT-RUNS EVERYTHING IT CHASES. 190mph against a sports class topping out at 188,
     which breaks the owner's standing rule that a cruiser is comparable to the sports class.
  3. THE SUPER CRUISER IS SLOWER THAN EVERY SUPERCAR. 184 against 194 to 206, and its own note claims
     the equipment costs it 4mph when the gap is 10. Either the note is wrong or the number is; this
     proposal makes the note true rather than rewriting it.
  4. THE PRODUCTION CARS CROWD THE SPORTS CLASS. COUPE at 160 against TUNER at 164.
  5. A SUPERCAR IS OUT-DRAGGED BY TWO ROAD CARS. STALLION measured 4.4s to 60 against MUSCLE and
     CRUISER at 4.2s. Long gearing explains it and no player will accept it.

THE PROPOSED TABLE. `accel` is the new name for `pull`, by the owner's ruling; the value means the
same thing and the column is renamed as part of applying this. An arrow shows a change, and "=" means
the number stays.

    class        body              mph            0-60          accel           brake         grip
    ------------------------------------------------------------------------------------------------
    FORMULA      COMET          276 =           2.3s =      1.56 =          1.82 =        1.92 =
                 APEX           260 =           2.1s =      1.70 =          1.90 =        2.00 =
                 VECTOR         248 =           2.0s =      1.86 =          1.95 =        2.05 =
    SUPER        STALLION       206 =           3.8s <- 4.2  0.94 <- 0.84   1.30 =        1.34 =
                 CREST          200 =           3.3s <- 3.5  1.08 <- 1.02   1.32 =        1.42 =
                 MATADOR        194 =           2.9s =      1.24 =          1.28 =        1.38 =
    POLICE       SUPERCRUISER   190 <- 184      3.2s <- 3.1  1.14 <- 1.18   1.44 =        1.36 <- 1.38
                 CRUISER        180 <- 190      3.9s <- 4.0  0.94 <- 0.92   1.10 <- 1.00  0.96 <- 0.92
    SPORTS       MUSCLE         178 <- 188      4.2s =      0.92 =          0.86 =        0.82 =
                 ROADSTER       172 <- 176      4.6s <- 5.1  0.80 <- 0.72   1.20 <- 1.12  1.34 =
                 TUNER          164 =           3.6s <- 3.2  1.04 <- 1.18   1.02 =        1.00 =
    PRODUCTION   COUPE          150 <- 160      6.4s =      0.62 =          0.88 =        0.86 =
                 SALOON         140 <- 148      7.5s =      0.54 =          0.80 =        0.78 =
                 CAB            126 <- 132      9.1s =      0.46 =          0.72 =        0.70 =
    UTILITY      PICKUP         128 <- 136      9.4s =      0.44 =          0.68 =        0.64 =
                 VAN            115 <- 120     12.2s =      0.36 =          0.62 =        0.58 =
                 LORRY           80 =         23.1s =       0.24 =          0.40 =        0.42 =

THE SHAPE THAT COMES OUT OF IT, which is the thing to approve or reject:

    formula     248 - 276      the prize, and 20% clear of the class below (RLG-070)
    super       194 - 206
    police      180 and 190    one seated in each of the two classes it polices
    sports      164 - 178      the starting class
    production  126 - 150
    utility      80 - 128

Every gap between classes is 14mph or more and no class overlaps another. The two police cars sit
inside the classes the owner's standing rule names: the CRUISER 2mph above the fastest sports car it
chases, and the SUPER CRUISER 4mph below the slowest supercar - which makes its own note about the
equipment costing it 4mph true for the first time.

REASONING, PER MOVE:

  MUSCLE 188 -> 178. The single change that fixes the overlap. It is still the fastest thing in the
  starting class and still the straight-line corner of that triangle; it is simply no longer within
  6mph of a supercar.

  ROADSTER 176 -> 172, accel 0.72 -> 0.80, brake 1.12 -> 1.20. It was the slowest car in its class
  off the line while carrying the best grip in it, which reads as a car that is bad at everything
  except a stat nobody can see. Light means it stops and turns, and it should also launch better than
  a muscle car of nearly twice its weight. 5.1s becomes 4.6s.

  TUNER accel 1.18 -> 1.04. It reached 60 in 3.2s - quicker than CREST at 3.5 and within a breath of
  MATADOR at 2.9 - from the STARTING class. 3.6s keeps it comfortably the quickest of its own class
  and puts a real step above it.

  STALLION accel 0.84 -> 0.94 and CREST 1.02 -> 1.08. This is fault 5. STALLION is still deliberately
  the slowest-launching supercar, which is its identity and what its note says, but it is no longer
  beaten off the line by a muscle car and a patrol car.

  CRUISER 190 -> 180, brake 1.00 -> 1.10, grip 0.92 -> 0.96. Seated in the sports class as ruled, and
  given the equipment a pursuit car has instead of the top speed it should not have. It still
  out-runs everything in the class it polices, by 2mph.

  SUPER CRUISER 184 -> 190, grip 1.38 -> 1.36, accel 1.18 -> 1.14. Seated with the supercars. A cage
  and a light bar cost it 4mph against the MATADOR it is built from, which is exactly what its note
  has always claimed.

  THE PRODUCTION AND UTILITY CARS come down by RLG-043's rule rather than by a flat percentage:
  `new = LORRY + (old - LORRY) * 0.88`. The fastest drops most and the lorry does not move, which is
  the whole point of the scheme. Their `accel`, `brake` and `grip` are untouched: those were measured
  honest and are already correctly ordered, and making a van worse at stopping is not a design goal.

TWO CONFLICTS NAMED RATHER THAN DECIDED QUIETLY, as the rule for this table requires:

  1. TUNER STILL BEATS STALLION TO 60 - 3.6s against 3.8s. Realism says a light tuned car out-drags a
     long-geared GT, and that is a real thing; the ladder says a class you paid for should win. It is
     kept, because both cars' identities depend on it: STALLION is the slowest-launching supercar and
     TUNER is the quickest-launching sports car, so the two were always going to meet. The
     alternative, if the owner wants the ladder to win, is STALLION at accel 1.00, which ties them at
     3.6s.
  2. THE PICKUP IS FASTER THAN THE CAB - 128 against 126, and it always was. Kept: a working pickup
     out-running a taxi with three hundred thousand miles on it is the more believable of the two,
     and the cab's own note says exactly that about itself.

WHAT THIS PROPOSAL DOES NOT TOUCH, and why:

  THE NPC TRAFFIC. Their speeds do not come from `BODY` - they are set where the traffic is spawned.
  RLG-042 established that and RLG-055 ruled the traffic half blocked until traffic reads the table,
  because a number nothing consults cannot be signed off as anything. The production and utility rows
  above are the DRIVEABLE versions only.

  THE FORMULA CLASS. Set and measured under RLG-070 at twenty per cent clear of the best supercar in
  every stat. Nothing here moves it.

  HOW HARD THE FIELD RACES. The owner ruled that NPCs do not get better cars and that a rival is
  competitive because it drives well (RLG-033). So every number above is argued on what the vehicle
  should be, and none of it is tuned to make a race close.

TO APPLY, once it is signed off: rewrite the sixteen `BODY` entries, rename `pull` to `accel`
throughout - twenty-eight references - and fix the three comments RLG-055 named: the rung comment
claiming MUSCLE 184 and TUNER 172, the supercar comment claiming anything above 1.0 is clamped, and
the SUPER CRUISER note, which becomes true rather than being rewritten.
- **RULED 2026-08-29T10:48:55-04:00** — REVISION 2, on the owner's instruction, 2026-08-29: bring the sports, production and utility classes
down by 10 to 20 per cent; make the gap between SUPER and SPORTS as large as the gap between FORMULA
and SUPER, and the gap from SPORTS to PRODUCTION the same again; PRODUCTION to UTILITY may be a
smaller step of 5 to 10 per cent.

THE GAP, MEASURED FROM THE ONE THAT ALREADY EXISTS. Formula's floor is 248 and super's ceiling is
206, so the step the owner is pointing at is 1.204 - about twenty per cent. Every boundary below is
built to that same ratio, and the classes fall where the arithmetic puts them:

    formula floor 248  /  super ceiling 206   =  1.204   (the reference)
    super   floor 194  /  sports ceiling 160  =  1.213
    sports  floor 146  /  production ceil 120 =  1.217
    production floor 100 / utility ceiling 94 =  1.064   (the smaller step, 6%)

THE PROPOSED TABLE. `accel` is the new name for `pull`. An arrow shows a change from what is in the
code today; "=" means the number stays.

    class        body            mph              0-60           accel          brake         grip
    -------------------------------------------------------------------------------------------------
    FORMULA      COMET        276 =            2.3s =        1.56 =         1.82 =        1.92 =
                 APEX         260 =            2.1s =        1.70 =         1.90 =        2.00 =
                 VECTOR       248 =            2.0s =        1.86 =         1.95 =        2.05 =
    SUPER        STALLION     206 =            3.8s <- 4.2   0.94 <- 0.84   1.30 =        1.34 =
                 CREST        200 =            3.3s <- 3.5   1.08 <- 1.02   1.32 =        1.42 =
                 MATADOR      194 =            2.9s =        1.24 =         1.28 =        1.38 =
    POLICE       SUPERCRUISER 190 <- 184       3.2s <- 3.1   1.14 <- 1.18   1.44 =        1.36 <- 1.38
                 CRUISER      164 <- 190       4.4s <- 4.0   0.86 <- 0.92   1.10 <- 1.00  0.96 <- 0.92
    SPORTS       MUSCLE       160 <- 188       4.7s <- 4.1   0.84 <- 0.92   0.77 <- 0.86  0.74 <- 0.82
                 ROADSTER     153 <- 176       5.2s <- 5.1   0.72 =         1.05 <- 1.12  1.20 <- 1.34
                 TUNER        146 <- 164       4.4s <- 3.2   0.92 <- 1.18   0.92 <- 1.02  0.90 <- 1.00
    PRODUCTION   COUPE        120 <- 160       7.9s <- 6.3   0.54 <- 0.62   0.75 <- 0.88  0.73 <- 0.86
                 SALOON       112 <- 148       9.6s <- 7.3   0.47 <- 0.54   0.68 <- 0.80  0.66 <- 0.78
                 CAB          100 <- 132      11.9s <- 8.9   0.40 <- 0.46   0.61 <- 0.72  0.60 <- 0.70
    UTILITY      PICKUP        94 <- 136      13.1s <- 9.2   0.38 <- 0.44   0.56 <- 0.68  0.52 <- 0.64
                 VAN           86 <- 120      17.1s <- 11.8  0.31 <- 0.36   0.51 <- 0.62  0.48 <- 0.58
                 LORRY         80 =          23.1s =         0.24 =         0.40 =        0.42 =

THE LADDER THAT COMES OUT OF IT:

    formula     248 - 276      2.0 - 2.3s
    super       194 - 206      2.9 - 3.8s
    police      164 and 190    4.4s and 3.2s   one seated in each class it polices
    sports      146 - 160      4.4 - 5.2s      the starting class
    production  100 - 120      7.9 - 11.9s
    utility      80 -  94     13.1 - 23.1s

"EVERYTHING" IS READ AS EVERY STAT, NOT ONLY SPEED. The three classes come down in top speed,
acceleration, braking and grip together, by about 10% for the sports class, 15% for production and
18% for utility. A class that is slower in a straight line but stops and turns as well as the class
above it has not moved down a rung - it has become the smart choice, which is the opposite of a
ladder. If the owner meant top speed alone, that is a smaller edit and the acceleration column would
have to come down anyway: a lower top end shortens the gear bands, which flatters 0-60 and would have
made these cars accidentally quicker off the line than they are today.

WHAT THAT DOES TO STOPPING DISTANCE, from 140 to 40mph on the measured relationship:

    SUPERCRUISER 338m   CRUISER 442m   ROADSTER 463m   TUNER 528m   MUSCLE 631m
    COUPE 648m   SALOON 715m   CAB 797m   PICKUP 868m   VAN 953m   LORRY 1215m

WHERE THE OWNER'S TWO INSTRUCTIONS PULL AGAINST EACH OTHER, AND IT MUST BE SAID PLAINLY:

  The 10-to-20% cut and the equal-gap rule cannot both hold for every class. Twenty per cent between
  each boundary compounds: the sports class falls 11 to 15%, which is inside the band, but production
  falls 24 to 25% and utility 28 to 31%, which is outside it. The gaps were followed, because they
  are the thing the owner described in detail and because they are what makes the ladder read as a
  ladder.

  THE ALTERNATIVE, if the 10-to-20% band matters more than the equal gaps: hold production at
  128-130 and utility at 108-116, which is a 12-15% cut and gives a step of about 12% rather than 20%
  between sports and production. Say the word and the table is rebuilt that way.

TWO CONSEQUENCES WORTH SEEING BEFORE SIGNING:

  1. A VAN NOW TAKES 17 SECONDS TO REACH 60 and tops out at 86mph, and a lorry is unchanged at 80.
     The gap between the slowest utility vehicle and the fastest is only 14mph, so the utility class
     is now much more about what the vehicle IS than about what it does. That is arguably correct for
     a van and a lorry, and it is the class most likely to feel like a punishment rather than a car.

  2. THE PICKUP IS NOW SLOWER THAN THE CAB - 94 against 100, where it used to be faster. The old
     conflict resolved itself: the pickup fell into the utility class and the cab stayed in
     production, which is the right side of the line for each of them.

WHAT IS UNCHANGED FROM REVISION 1: the formula class (set under RLG-070 and 20% clear of the best
supercar), the supercars, the seating rule for the two police cars, and the fact that no number here
is tuned to make a race close - a rival is competitive because it drives well (RLG-033).

THE LORRY STILL DOES NOT MOVE. That is RLG-043's rule and it survives the rescale: the normalization
pulls the fastest of a class down hardest and leaves the slowest where it is.

NOTHING IS APPLIED. `BODY` is untouched until this is signed off.
- **RULED 2026-08-29T10:55:33-04:00** — REVISION 3, on the owner's two amendments, 2026-08-29. This is the sign-off copy.

  "The cruiser should be slightly worse than the sports cars in the same vein that the super cruiser
   is slightly worse than the supercars."

  "I am also OK if we artificially bump up the acceleration for the utility vehicles a bit so they
   are not so ludicrously slow off the line."

1. THE CRUISER SITS BELOW ITS CLASS, EXACTLY AS THE SUPER CRUISER DOES.

The super cruiser's relationship to the supercars, measured: 190mph against a class floor of 194 -
four miles an hour below the slowest of them - while its 0-60 of 3.2s sits INSIDE the class rather
than behind it, and its brakes are the best of the four. That is the shape of a police car: it is not
the fastest thing on the road, it is the thing that is still there three corners later.

The cruiser now has the same shape against the sports class: 142mph against a floor of 146, again
four below the slowest, with a 0-60 of 4.9s that sits inside the class spread of 4.4 to 5.2, and the
best brakes in it.

    CRUISER   164 -> 142 mph,  accel 0.86 -> 0.82,  0-60 4.4s -> 4.9s
              brake 1.10 and grip 0.96 are unchanged from revision 2 - the equipment stays

This costs the cruiser the ability to simply out-drive what it chases, which is the point of the
amendment and is worth stating: a pursuit is now something the police car has to WIN rather than
something it wins by having a bigger engine. That is the same principle as RLG-033 - a car is
competitive because of how it is driven.

2. THE UTILITY VEHICLES LEAVE THE LINE BETTER THAN THEIR TOP SPEED SUGGESTS.

    PICKUP   accel 0.38 -> 0.52,  0-60 13.1s -> 9.7s
    VAN      accel 0.31 -> 0.44,  0-60 17.1s -> 12.2s
    LORRY    accel 0.24 -> 0.30,  0-60 23.1s -> 18.6s

The owner called this artificial and offered it as a concession to fun. It is worth recording that it
is not artificial at all, because that matters for every number set here later: `accel` is TORQUE, and
torque is precisely what a working vehicle has. A diesel van or a lorry pulls hard from low revs and
then runs out of gearing and pushes a barn door through the air - strong off the line, poor at the
top end - which is exactly the shape these three now have. The realistic version and the fun version
agree here; the numbers were wrong before, rather than being made wrong now.

The ordering across the two classes still holds and was checked: COUPE 7.9s, SALOON 9.6s, PICKUP
9.7s, CAB 11.9s, VAN 12.2s, LORRY 18.6s. The lorry remains, by a distance, the slowest thing anybody
can drive.

THE FINAL TABLE.

    class        body            mph              0-60           accel          brake         grip
    -------------------------------------------------------------------------------------------------
    FORMULA      COMET        276 =            2.3s =        1.56 =         1.82 =        1.92 =
                 APEX         260 =            2.1s =        1.70 =         1.90 =        2.00 =
                 VECTOR       248 =            2.0s =        1.86 =         1.95 =        2.05 =
    SUPER        STALLION     206 =            3.8s <- 4.2   0.94 <- 0.84   1.30 =        1.34 =
                 CREST        200 =            3.3s <- 3.5   1.08 <- 1.02   1.32 =        1.42 =
                 MATADOR      194 =            2.9s =        1.24 =         1.28 =        1.38 =
                 SUPERCRUISER 190 <- 184       3.2s <- 3.1   1.14 <- 1.18   1.44 =        1.36 <- 1.38
    SPORTS       MUSCLE       160 <- 188       4.7s <- 4.1   0.84 <- 0.92   0.77 <- 0.86  0.74 <- 0.82
                 ROADSTER     153 <- 176       5.2s <- 5.1   0.72 =         1.05 <- 1.12  1.20 <- 1.34
                 TUNER        146 <- 164       4.4s <- 3.2   0.92 <- 1.18   0.92 <- 1.02  0.90 <- 1.00
                 CRUISER      142 <- 190       4.9s <- 4.0   0.82 <- 0.92   1.10 <- 1.00  0.96 <- 0.92
    PRODUCTION   COUPE        120 <- 160       7.9s <- 6.3   0.54 <- 0.62   0.75 <- 0.88  0.73 <- 0.86
                 SALOON       112 <- 148       9.6s <- 7.3   0.47 <- 0.54   0.68 <- 0.80  0.66 <- 0.78
                 CAB          100 <- 132      11.9s <- 8.9   0.40 <- 0.46   0.61 <- 0.72  0.60 <- 0.70
    UTILITY      PICKUP        94 <- 136       9.7s <- 9.2   0.52 <- 0.44   0.56 <- 0.68  0.52 <- 0.64
                 VAN           86 <- 120      12.2s <- 11.8  0.44 <- 0.36   0.51 <- 0.62  0.48 <- 0.58
                 LORRY         80 =          18.6s <- 23.1   0.30 <- 0.24   0.40 =        0.42 =

The two police cars are listed inside the classes they belong to rather than as a class of their own,
because that is now what they are: each one is the slowest car of its class and the best-braked.

    formula     248 - 276      2.0 - 2.3s
    super       190 - 206      2.9 - 3.8s     the super cruiser is the floor of it
    sports      142 - 160      4.4 - 5.2s     the cruiser is the floor of it; the starting class
    production  100 - 120      7.9 - 11.9s
    utility      80 -  94      9.7 - 18.6s

Boundary ratios, against the 1.204 the formula class already sets: super to sports 1.213, sports to
production 1.217, production to utility 1.064.

EVERYTHING ELSE IS AS REVISION 2, including the two places where the owner's instructions pulled
against each other and the gaps won: production falls 24-25% and utility 28-31% in top speed, outside
the 10-to-20% band, because twenty per cent per boundary compounds.

NOTHING IS APPLIED. `BODY` is untouched until this is signed off.
- **RULED 2026-08-29T11:10:59-04:00** — REVISION 4, on the owner's design note, 2026-08-29:

  "Acceleration is a function of horsepower and mass. Braking is a function of grip and mass (and
   admittedly the mechanical brakes too)."

THE OWNER IS RIGHT ABOUT ACCELERATION, AND THE CODE ALREADY SAYS SO IN ONE PLACE. The launch kick at
road.js:5484 carries the comment "power against MASS, which is what actually decides this. `pull` was
a stand-in until mass existed; it is not one any more" - and it computes `powerToWeight()` rather than
reading the stat. One expression made this move already. This finishes it.

THE CORRECTION ON BRAKING, and it matters because it decides whether a term goes in or stays out.
IN IDEAL BRAKING, MASS CANCELS. A tyre-limited stop decelerates at the coefficient of friction times
gravity - `a = mu * g` - and the mass appears on both sides of the equation and falls out. A heavy car
needs more force to stop, and its weight presses the tyres down proportionally harder to provide it.
That is why a loaded lorry and an empty one stop in similar distances, and why a heavy saloon can
out-brake a light one on the same tyres.

What DOES make a heavy vehicle stop badly is not its mass directly: it is brake capacity relative to
mass, tyre load sensitivity - a tyre loses grip per kilogram as the load rises - and, in a lorry's
case, the fact that its tyres are hard compounds built for wear.

ALL THREE OF THOSE ARE ALREADY IN `grip`. The lorry's grip is 0.42 against a supercar's 1.34, and
that is where its 1215-metre stop comes from. Adding a second mass term to braking would count the
same physics twice and make the heavy vehicles undriveable.

The measurement supports this rather than my saying it. Fitting `brake / grip` across the whole fleet
gives a factor between 0.88 and 1.15, with NO TREND AGAINST MASS: the lorry reads 0.95 at 14,000kg
and a formula car reads 0.95 at 690kg; the two highest are the police cars at 1.06 and 1.15, which is
their equipment, and the lowest is the ROADSTER at 0.88. That residual is the mechanical brakes - the
part the owner named last and the only part left once grip has done its work.

THE MODEL:

    accel  =  1.56 * sqrt(hp / mass) * launch          launch defaults to 1.00
    brake  =  grip * mech                              mech defaults to 1.00

WHY A SQUARE ROOT, rather than the linear hp/mass the owner's sentence implies. A linear model says a
formula car at 1.42 hp/kg accelerates three times as hard as a supercar at 0.47 - which would put it
at 0-60 in about 1.2 seconds against the supercar's 3.8. Reality does not do that: a real single-seater
does 0-60 in about 2.5s and a real supercar in about 2.9. The reason is TRACTION AND GEARING - a car
cannot deploy power it cannot put down, and the more power per kilogram it has, the smaller the
fraction of it that reaches the road off the line. A square root is the standard compression for
exactly that, and it is what the fitted data asks for independently: fitting the exponent freely gives
0.473, and the difference between that and a clean 0.5 is absorbed by `launch`.

`launch` IS GEARING AND TRACTION, and it is what makes three cars with the same engine and the same
weight different. The three formula cars have power-to-weight within half a per cent of each other, and
their whole trade is gearing: VECTOR 1.00, APEX 0.92, COMET 0.84 - shorter gears for the launch, taller
for the top end. The same shape appears in the supercars, MATADOR 1.20 against STALLION 0.88, which is
precisely the difference their notes have always claimed.

THE AUTHORED TABLE BECOMES THIS. Four physical facts and two bounded character factors, and the two
stats that were free-floating multipliers are now derived:

    body            hp    mass   grip   vmax   launch   mech      -> accel   brake
    ------------------------------------------------------------------------------
    VECTOR         980     690   2.05   1.24     1.00   0.95         1.86    1.95
    APEX          1020     720   2.00   1.30     0.92   0.95         1.71    1.90
    COMET         1080     760   1.92   1.38     0.84   0.95         1.56    1.82
    STALLION       710    1520   1.34   1.03     0.88   0.97         0.94    1.30
    CREST          640    1450   1.42   1.00     1.04   0.93         1.08    1.32
    MATADOR        690    1580   1.38   0.97     1.20   0.93         1.24    1.28
    SUPERCRUISER   690    1770   1.36   0.95     1.17   1.06         1.14    1.44
    CRUISER        370    1810   0.96   0.71     1.16   1.15         0.82    1.10
    MUSCLE         480    1720   0.74   0.80     1.02   1.04         0.84    0.77
    ROADSTER       240    1010   1.20   0.765    0.95   0.88         0.72    1.06
    TUNER          320    1290   0.90   0.73     1.18   1.02         0.92    0.92
    COUPE          210    1340   0.73   0.60     0.87   1.03         0.54    0.75
    SALOON         160    1480   0.66   0.56     0.92   1.03         0.47    0.68
    CAB            130    1620   0.60   0.50     0.91   1.02         0.40    0.61
    PICKUP         220    2150   0.52   0.47     1.04   1.08         0.52    0.56
    VAN            140    2400   0.48   0.43     1.17   1.06         0.44    0.51
    LORRY          420   14000   0.42   0.40     1.11   0.95         0.30    0.40

IT REPRODUCES THE SIGNED-OFF TABLE TO WITHIN 0.6% ON EVERY CAR. That is the point of doing it this
way round: the feel the owner approved in revision 3 does not change at all, and what changes is that
the numbers now FOLLOW from horsepower, mass and grip instead of sitting beside them. The physical
model and three revisions of hand-tuning agree with each other, which is the strongest evidence
either of them was going to get.

WHAT THIS COSTS, and it should be said before signing:

  HORSEPOWER AND MASS BECOME LOAD-BEARING. They are printed on cards today and affect almost nothing;
  after this, editing either one changes how the car drives. That is the intended benefit and it is
  also the risk: a plausible-looking edit to a spec sheet is now a change to the game.

  THE CHARACTER FACTORS MUST STAY BOUNDED, or they become `pull` again under a new name. The fleet
  spans 0.84 to 1.20 on `launch` and 0.88 to 1.15 on `mech`. Anything outside about 0.80 to 1.25 is
  the model telling you the horsepower or the mass is wrong, and it should be read that way rather
  than dialled around.

  GRIP STAYS AUTHORED. It could be derived in turn - tyre quality against mass - but grip is where
  tyre, suspension and aerodynamics all land, and the load sensitivity that makes a lorry slide is
  the thing it is already expressing. Deriving it would need a fifth authored number to replace it.

TO APPLY, once signed off: `accelOf(k)` and `brakeOf(k)` replace `pullOf(k)` and `BODY[k].brake`;
`bodyStat('pull')` and `bodyStat('brake')` become those two calls; the `pull` and `brake` columns come
out of all seventeen entries and `launch` and `mech` go in; `zeroSixty` reads the derived value. The
harness then has to show the same numbers it showed before - the fourth experiment measures braking
distance and cornering directly, so a derivation that changes behaviour will be visible rather than
argued about.
- **RULED 2026-08-29T11:11:24-04:00** — APPLIED, 2026-08-29. The owner: "I would like to retain the functional stats we have landed at but
using that machinery." So the signed-off table of revision 3 is what the fleet now does, and
revision 4's derivation is how it says it.

WHAT CHANGED IN THE CODE:

  `accelOf(k)` and `brakeOf(k)` are the two derived stats. `pullOf` survives as a one-line alias
  because the rival code calls it by that name, and renaming a call site is not the same job as
  changing what it returns.

  The `pull` and `brake` columns are GONE from all seventeen bodies. `launch` and `mech` replace
  them: bounded character factors, 0.84 to 1.20 and 0.88 to 1.15 across the fleet.

  Four consumers were repointed - `zeroSixty` (the garage card), the player's acceleration term, the
  player's brake rate, and the rival grid, which was reading `B.pull` off a table that no longer has
  it. That last one is the reason this was done in one unit: a rival built from a missing field gets
  `undefined` and accelerates at nothing, and no check in this project would have caught it.

  `stat-test` reads the derived stats through the probe now. It was reading `B.brake`, which after
  the change is `undefined || 1` for every car - a table of ones, and nothing throws. That is what a
  harness reading a stat that no longer exists looks like, and it is worth naming because it looks
  exactly like a fleet with no braking differences at all.

THE THREE COMMENTS RLG-055 NAMED ARE FIXED, and one of them was fixed by the numbers rather than by
the prose:

  The supercar comment claimed 200mph is the ceiling and that anything above 1.0 is clamped. Neither
  is true: `vmax` is a multiple of 200, the formula class runs to 1.38, and both real clamps were
  derived from the fleet under RLG-075.

  The rung comment claimed MUSCLE 184 and TUNER 172. It now carries the measured figures and says
  that the triangle survived the rescale, which is why the class could be moved as a unit.

  The SUPER CRUISER's note says it is a MATADOR with a cage in it, and the old numbers had it ten mph
  slower. At 190 against MATADOR's 194 the note is true, so it stays and the comment beside it
  explains why the number moved to meet it.

VERIFIED, and by measurement rather than by reading:

  Braking, from the fourth experiment, after the change: VECTOR 250m, APEX 254, COMET 264, STALLION
  372, MATADOR 377, CREST 366, CRUISER 436, ROADSTER 460, TUNER 528, COUPE 561, SALOON 613, MUSCLE
  626, CAB 679, PICKUP 751, VAN 811, LORRY 1024. The order is the table's order and the spread is
  four to one.

  Cornering, same run: the measured `cornerG` times the declared grip is 0.419 with a standard
  deviation of 0.006 across the fleet, against the 0.42 the engine defines. Grip still does exactly
  what it says after the rescale.

  Smoke 6/6 and `pack.sh --check` clean.
- **RULED 2026-08-30T11:44:53-04:00** — THE MEASUREMENT RLG-055 WAS BLOCKED ON EXISTS AND HAS NOW BEEN RUN. `stat-test --only stopturn` is
the fourth experiment, and it was built rather than missing - what had never happened was somebody
running it and writing the numbers down. NO NUMBERS ARE PROPOSED HERE. The owner asked for a
measurement before a proposal and this is the measurement.

BRAKE, from 140mph to 40mph with no throttle, best of two, in cleared air. Distance is the figure
that matters, because a corner arrives at a place rather than at a moment.

    VECTOR 249m   APEX 253m   COMET 263m        the formula class, 0.43-0.45s
    STALLION 370m   MATADOR 375m   CREST 461m   the supercars
    CRUISER 436m   ROADSTER 460m   TUNER 529m
    MUSCLE 631m   COUPE 554m   SALOON 586m
    CAB 688m   PICKUP 769m   VAN 941m   LORRY 1159m

The ordering is what anyone would expect and the SPREAD is the part worth having: a lorry needs four
and a half times a formula car's road to do the same job, and nearly twice a roadster's.

GRIP, driven into one bend at 120mph with NOBODY TOUCHING THE WHEEL. That is the whole design of the
experiment and the reason it can be trusted: lateral load is what wears a tyre and moves a car, so a
driver in the loop is the thing being measured. The project has already been told once that Raceway's
tyres died in twenty seconds when the cause was the drive-test autopilot sawing at the wheel.

AND THE GRIP RESULT IS NOT A CLEAN PASS, which is the finding. The engine defines the cornering push
as `0.42 / grip`, so the measured push multiplied by the declared grip should read 0.42 for every car.
Measured: mean 0.441, spread 0.410 to 0.555, sd 0.045.

Eleven of the sixteen sit between 0.410 and 0.419, which is the definition holding exactly. The five
that do not are CREST at 0.555 and the four heaviest road cars - CAB 0.463, PICKUP 0.477, VAN 0.492,
LORRY 0.522.

THE LIKELIEST EXPLANATION IS THE INSTRUMENT, NOT THE ENGINE, and it is stated here rather than
assumed either way: every row ended with "left the bend". A low-grip car drifts across the road
faster, so its integration window is shorter and weighted toward the start of the push, where the
transient is. The cars that read high are exactly the cars that leave soonest. That is a hypothesis
with a test - hold a longer bend, or clip the first tenth of a second out of the integration - and
neither has been run.

WHAT THIS UNBLOCKS. RLG-055 can be written against measured brake and measured grip rather than
against declared ones, with the caveat above attached to the five cars it applies to. What it must
not do is treat 0.441 as a target to tune toward: the number to trust is 0.42, which is the engine's
own definition, and the deviation is more likely to be in this measurement than in the road.
- **RULED 2026-08-31T12:56:39-04:00** — MEASURED, 2026-08-31. `tools/brake-test.py` is the fourth experiment this ruling has been blocked on
since 2026-08-28. It proposes no numbers and changes no stat - it reports what the cars do.

THE BRAKING TABLE IS HONEST, AND THAT IS THE HEADLINE. Braking from 120 to 40mph, dry, on a flat
straight road, every driveable body:

    body           time     distance    brakeOf
    VECTOR          381 ms    2121        1.947
    APEX            532       2221        1.900
    COMET           414       2270        1.824
    SUPERCRUISER    622       2905        1.442
    CREST           684       3141        1.321
    STALLION       1102       3264        1.300
    MATADOR         666       3286        1.283
    CRUISER        1223       3783        1.104
    ROADSTER        749       3946        1.056
    TUNER          1227       4550        0.918
    MUSCLE         1136       5449        0.770

ELEVEN OF ELEVEN IN THE ORDER THE TABLE CLAIMS. Every car the table says brakes better actually stops
shorter, with no inverted pair anywhere in the fleet. That is the opposite of what RLG-042 found for
the garage card, and it is worth saying plainly: THE BRAKE COLUMN NEEDS NO CORRECTION. The spread is
2.6 to 1 from the VECTOR to the MUSCLE.

AND THE FIRST RUN SAID OTHERWISE, WHICH IS THE PART TO REMEMBER. It entered at 150mph and reported two
inverted pairs, with the TUNER stopping shorter than two cars the table says brake better. The TUNER's
top speed is 146. Above a car's own top end the engine applies aero over-run at a fixed 5,200 - "aero
drag above the limiter is brutal" - so the TUNER was being slowed by something that is not its brakes.
The entry speed is 120 now, below every driveable body's top end. A measurement taken above a car's
capability measures the limiter.

THE CORNERING RESULT IS DIRECTIONAL AND IS NOT YET TRUSTWORTHY, and it is reported that way rather
than dressed up. On one constant bend at 120mph with the wheel untouched, pushed wide over 2.5s:

    VECTOR    0.073   grip 2.05        MATADOR   0.109   grip 1.38
    COMET     0.078        1.92        SUPERCR.  0.112        1.36
    MATADOR   0.109        1.38        STALLION  0.113        1.34
    ROADSTER  0.110        1.20        CRUISER   0.159        0.96
    TUNER     0.167        0.90        MUSCLE    0.203        0.74
    CREST     0.246        1.42        APEX      0.294        2.00

THE TREND IS RIGHT AND TWO CARS ARE NOT. Low-grip bodies are pushed most and the formula cars least,
which is what `cornerG` should produce. But the CREST at 0.246 and the APEX at 0.294 are the two
highest readings in the fleet while carrying two of the highest grip figures. That is either a real
fault or an artefact of this experiment, and NOTHING HERE SAYS WHICH.

WHAT THE EXPERIMENT ALREADY HAD TO BE FIXED FOR, so the next attempt does not repeat it. It compared
the push against `brakeOf`, which is grip times mech, while `cornerG` reads grip ALONE - that reported
31 inverted pairs and was arithmetic rather than an engine fault. And every body met a different bend,
because the road is generated per load and advances while the run proceeds: `API.bendRoad` gives one
constant curvature so the only thing differing between bodies is the body. The first body measured
also read exactly 0.0000, because its window closed before the imposed corner reached the car; there
is a settle now. Three faults in one experiment, all mine, all found by the numbers looking wrong.

SO THE CORNERING HALF IS NOT SIGNED OFF AND SHOULD NOT BE TUNED AGAINST. What it can support is the
weaker, useful claim: grip is doing SOMETHING in the corner, the fleet spreads 0.073 to 0.294, and the
ordering is broadly by grip with two exceptions that need their own look.

AND THE WHEEL WAS NEVER TOUCHED, which was this ruling's own warning. Grip must not be measured with
the drive-test autopilot because it saws at the wheel and lateral load is what wears tyres. There is
no driver in this experiment at all: `cornerG` moves `targetX` on its own, and the measurement is that
drift. Nothing a driver does can influence it because nothing drives.

WHAT IS STILL NOT MEASURED, stated so it is not assumed. Tyre wear against grip. The traffic bodies,
which is the half RLG-043 left blocked until traffic reads `BODY` - it now reads `TYPE_VMAX`, which is
a different table, so that blocker still stands. And whether the spread in either column is large
enough to FEEL, which is a device question and the owner's.

TWO ENGINE FACTS ESTABLISHED BY READING, BOTH WORTH KEEPING. `grip` is consulted in exactly two
places: `brakeOf`, which scales the braking rate, and `cornerG`, which scales how hard a bend pushes
you wide. It does NOT affect how fast the car answers the wheel - that rate is identical for every
body and is moved only by the weather. The owner has since asked whether it should, which is
[[RLG-119]].

A HARNESS TRAP FOUND ON THE WAY, recorded because it will catch somebody again. `playerX`, `targetX`
and `dmg` are live GETTERS on the API - properties, not calls - and a function of the same name
defined later in the file is silently overwritten by them. `API.targetX` read as a NUMBER, and at rest
that number is zero, so this harness's own "is this call present" guard saw a falsy value and reported
that the engine had no such API.
- **RULED 2026-08-31T16:54:32-04:00** — THE CORNERING HALF IS RESOLVED, 2026-08-31, and it was the instrument in three separate ways. This ruling reported the result as "directional and not yet trustworthy" and said plainly that the CREST and the APEX were "either a real fault or an artefact of this experiment, and NOTHING HERE SAYS WHICH." It was the experiment, and the engine's definition was right all along.

WITH THE THREE FAULTS FIXED, push x grip is ONE NUMBER for the whole fleet: mean 0.2278, 0.2279 and 0.2280 across three runs, spreading 0.1 to 1.3 per cent within a run. Four significant figures, reproducible. The engine defines the push as `CORNER_G_BASE / grip` and that is exactly what it does.

FAULT ONE: IT WAS MEASURING COLLISIONS. The experiment ran on Interstate, which has civilian traffic on it, and a car that gets HIT is shoved sideways and slowed at the same time - `hurt` pushes `playerX` and then sets `targetX = playerX`, which is the exact number this experiment reads as a cornering push. So a shunt was recorded as an enormous push, on a different body every run. That is why the CREST and the APEX looked like they defied their own grip, and why the STALLION read 0.113 in one run and 0.261 in another on an unchanged build. THE SPEED COLUMN GAVE IT AWAY THE MOMENT IT WAS PRINTED: four bodies went 119 to 47mph inside two and a half seconds, which is not coasting. The road is swept every frame now, from inside the probe - clearing it once is not enough, because Interstate spawns more inside the window every time.

FAULT TWO: THE SPEED WAS LEFT TO COAST. The push is `pushK * v * v * dt * cornerG()` - quadratic in speed - and the harness set 120mph once and then let the car roll for two and a half seconds. Every body was therefore pushed by a different v, and a 20 per cent speed loss is a 36 per cent push loss. The entry speed is re-asserted every frame now. That is not a driver influencing the result; it is the one variable that has to be equal between bodies for the comparison to be about grip at all.

FAULT THREE, AND IT IS THE ONE WORTH REMEMBERING: THE WINDOW WAS MEASURED AGAINST THE WALL CLOCK. `frameLoop` caps its own `dt` at 0.05, and the harness browser runs at about eleven frames a second - so the world simulates at roughly half real time, and at a DIFFERENT fraction for each body, because a heavier car renders slower. Two and a half seconds of wall time was a different length of simulated time for every car in the fleet. The engine publishes `API.simTime()` now, which is the seconds the world has actually been stepped, and both experiments measure against it.

AND A FOURTH, SMALLER: THE FIRST BODY MEASURED IS MEASURED DIFFERENTLY. The run has only just begun, so the car is still coming up to speed and the imposed bend has only just been laid. This file was already bitten by that once - the first body read exactly 0.0000 because its window closed before the corner reached it, and a settle was added. The settle was not enough: one run showed the ROADSTER ENTERING its window at 49mph. There is a throwaway warm-up window before the loop now, and the entry speed is checked as well as the exit, because a check that only looked at the exit passed that run.

THE CHECK ITSELF WAS THE WRONG QUESTION and has been replaced. It counted INVERTED PAIRS in the drift ordering, which punishes two cars whose grip differs by three per cent - the VECTOR at 2.05 against the APEX at 2.00 - for swapping places on measurement noise. What the engine actually defines is that push x grip is constant, so that is what is asserted, and it reports how far out the worst car is rather than merely that something is. The threshold is 5 per cent, four times the worst run seen. It was 16 per cent while the instrument was still measuring collisions, and left there it would have passed a real regression without a murmur.

THE BRAKING HALF IS UNCHANGED AND WAS NEVER IN DOUBT. Its distances are identical to the ones recorded here - 3946, 4550, 5449, 3217, 3238, 3141, 2121, 2198, 2294, 3808, 2905 - and it still ranks 11 of 11 in the order the table claims. NOTE FOR WHOEVER TOUCHES IT NEXT: its printed TIMES are still wall-clock and swing about 5 per cent run to run for exactly the reason above, while its DISTANCES are simulated and do not. The check reads the distances, so the result is sound; the times are decoration and should be moved to `simTime` when somebody is in there anyway.

SO RLG-055 CAN NOW BE WRITTEN AGAINST MEASURED CORNERING as well as measured braking, with no caveat attached to any car. There is no fleet-wide correction to make: every body corners exactly as its declared grip says it should.
- **RULED 2026-08-31T17:09:52-04:00** — A FIFTH FAULT IN THE SAME EXPERIMENT, found while building the steering measurement beside it and fixed in both: THE WEATHER ROLLS WHILE THE RUN PROCEEDS. Both experiments set the road dry once before their loop, and a full pass over the fleet takes about two minutes - long enough for rain to arrive on its own.

IT MATTERS BECAUSE `wetGrip` IS THE OTHER THING THE LATERAL PHYSICS READS. Wet makes `slideX` CARRY, so the car keeps going the way it was going and moves FASTER than the steering ceiling allows. The signature is unmistakable once the numbers are laid out in the order they were taken: the steering error grew steadily down the body list, 1.7 per cent on the first car and 28 per cent on the last. That is a drift over the RUN, not a property of any car - and read as a per-car result it would have been a false finding about the CRUISER and the SUPERCRUISER.

Both probes hold the road dry every frame now, and both REPORT the grip they ran under, so a row taken on a damp road is named and dropped rather than averaged in.

WITH ALL FIVE FIXED, both experiments are reproducible: push x grip reads mean 0.2278 and 0.2279 with a spread of 0.3 and 0.2 per cent, and every driveable body answers the wheel within 0.4 per cent of the ceiling the engine declares for it.

THE PATTERN ACROSS ALL FIVE IS ONE SENTENCE, and it is worth more than the numbers: EVERY CONTROLLED VARIABLE HAS TO BE HELD EVERY FRAME, not set once before the loop. The speed, the road being empty, the road being dry, and the clock the window is measured against. Four of the five faults were something set once at the top that the running game quietly took back.
