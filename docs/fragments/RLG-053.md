+++
id                = "RLG-053"
kind              = "ruling"
type              = "defect"
status            = "requested"
created           = "2026-08-28T18:02:24-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### RLG-053 — Every lamp is the unlit bulb drawn again, lit - not its own geometry
- **RULED 2026-08-28T18:03:02-04:00** — **Owner, 2026-08-28:** "The turn signal needs to illuminate like a brake light, but not as vibrantly.
Per our lessons learned, duplicate the unlit bulb as the lit bulb overlay so the dimensions and
locations are pixel perfect. This needs to happen across the board - every vehicle in the game:
headlights, brake lights, police light bar front right, front left, rear right, rear left, turn
signal lights."

**This is a technique ruling, and it applies to every lamp on every vehicle.** A lit lamp is the
UNLIT BULB DRAWN AGAIN, lit. It is never a rectangle with its own coordinates.

**The engine does the wrong thing today and it is easy to show.** `playerBrakes` at road.js:8229
computes its own geometry from the bounding box:

    const lw = box.w*0.265, lh = box.h*0.11;
    const ly = box.y - box.h*0.34;
    for(const lx of [left + box.w*0.135, left + box.w*0.60])

Not one of those numbers comes from wherever the car painter actually put the tail lamp. They are a
second, independent description of the same object. **So the lit lamp and the unlit lamp are two
drawings that happen to agree**, and they agree only for as long as nobody edits either one. Every
reskin is an opportunity for them to drift, silently, with no error and nothing failing.

This is the same class of fault the project already carries notes about - a badge drawn before the
band that then covered it, a fill that stopped 0.045h short and left a visible seam. The fix the
owner has ruled removes the class rather than the instance: if the lit lamp IS the unlit bulb, drawn
with a different fill, the two cannot drift, because there is only one of them.

**Scope, as the owner listed it.** Every vehicle: headlights, brake lights, the police bar as four
separately addressable lamps - front left, front right, rear left, rear right - and the turn signals
[[RLG-052]] will add. The bar's four corners being individually addressable is what lets a light bar
run a real pattern instead of pulsing as one blob, and it is what a stopped car with a cruiser
behind it will need in [[RLG-047]].

**"Like a brake light, but not as vibrantly"** is the intensity ruling for the indicator
specifically. The brake lamp currently sits at alpha 1.0 while braking against 0.34 for the running
light; the indicator belongs between them, and the exact value is a look decision for the owner on a
real device rather than a number to pick here.

**The size of this is the honest problem with it.** There are painters for supercars, road cars, the
cruiser, the super cruiser, and every traffic body, plus the rear-view and front sprites. This is not
one edit. **The first unit should be ONE vehicle end to end** - lamps declared once by the painter,
consumed by the lit overlay - so the shape is proved before it is repeated fifteen times. The record
should say which vehicle was done and which were not, at every step, so a half-converted engine is
never mistaken for a finished one.
- **RULED 2026-08-28T18:07:02-04:00** — **Amendment from [[RLG-052]], owner 2026-08-28.** The player's car declares indicator bulbs like every
other vehicle, and nothing ever lights them - there is no control and no automatic signalling.

**This is a useful constraint on the technique rather than an exception to it.** It forces the two
halves apart at the seam the owner already ruled: the painter DECLARES lamps, a separate overlay
CONSUMES them and lights the ones that are on. If the design cannot express "this bulb exists and is
never lit" without a special case in the painter, the split is in the wrong place. The player's
indicators are the cheapest available test of that, so **do them in the first vehicle converted.**
- **RULED 2026-08-28T18:09:39-04:00** — **Amendment from [[RLG-052]], owner 2026-08-28, replacing the previous amendment.**

The earlier note here said the player's indicators are declared and never consumed, and used that as
a test of the painter/overlay split. **The owner has since ruled that every vehicle's indicators are
wired to FUNCTION** - racers and the player included. Nothing triggers them on those two, but the
path works.

**The test this fragment wanted is therefore a better one than the one it had.** It is no longer
"can the design express a bulb that is never lit" - that is satisfied by doing nothing. It is:

> **every vehicle's lamps must be drivable by the same code, and the only difference between a
> traffic car and a racer is what asks the lamp to come on.**

So the first vehicle converted should have its indicators driven end to end and proved to light,
even though nothing in the game will ask them to. If lighting a racer's indicator needs code that
lighting a traffic car's does not, the split is wrong and it will be cheaper to find out on vehicle
one than on vehicle fifteen.

**This applies to the whole lamp set, not just the indicators.** Headlights, brake lights and the
four corners of the police bar are the same shape of thing: declared once by the painter, lit by a
consumer that does not care what kind of vehicle it is looking at. The police bar is the case that
will expose a bad seam fastest, because it is the only lamp set whose four members are addressed
independently.
- **RULED 2026-08-29T07:06:03-04:00** — **PART ONE BUILT in v0.9.22: the MATADOR, end to end, and only the MATADOR.**

**The seam, as the owner ruled it.** A painter DECLARES its lamps as functions that draw themselves
and take one argument - whether they are lit. `sprite()` runs each one unlit as it bakes the canvas
and keeps them on it. `lampsHere` runs the same function again, on the screen, through a transform
that maps the sprite onto the car as drawn. **There is one description and it is executed twice**, so
the lit lamp cannot be anywhere but exactly where the unlit bulb is, and a reskin moves both because
there is only one of them.

**The fault was worse than this fragment recorded. There were THREE descriptions of the player's
tail lamps, not two, and one of them was unreachable.**

| where | geometry | state |
|---|---|---|
| the sprite, from `paintCar` | the body's own tail art | the real one |
| `tailLights` | `0.265w x 0.11h` at `0.135` and `0.60` | live, for traffic |
| `playerBrakes` | the same numbers again | **dead - nothing called it** |
| inline in `drawPlayer` | circles at `p.x +/- 0.24w`, `p.y - 0.34h` | live, and what you actually saw |

`playerBrakes` is deleted rather than converted: converting it would have kept a second answer alive.
The inline copy remains ONLY for bodies that have not been converted, and its comment now says so.

**A fourth fault fell out of it.** The inline glow was drawn OUTSIDE the transform that draws the
car, so it did not lean with the car. The declared lamps are lit inside that transform, so they roll
with it - which nobody had noticed because the glow was a soft circle.

**Indicators exist and are wired, on this car, today.** [[RLG-052]]'s refined ruling is that every
vehicle's signals FUNCTION and only the driver differs. The MATADOR now carries an unlit amber bulb
at the outer end of each blade housing, `API.signal` lights it, and `blinkPhase` runs at about 1.5Hz
whether or not anything is signalling - so a lamp that comes on is already in step with every other
one on the road, which is what will make a line of traffic look like traffic. Nothing in the game
asks for it. That is the ruling, not an omission.

**`tools/lamp-test.py` is new, and the first two versions of it were bad in an instructive way.**

The obvious test is to light the lamp and diff the frame. It is not a test: the sky turns, the tail
glow follows the daylight and the car breathes, so **800 to 1,300 pixels of the car change between
two frames with the lamp OFF**, against a few hundred from the lamp. A control diff removes most of
that and not reliably - the same check reported **100% and then 35%** on consecutive runs, which is a
harness reporting weather.

The claim does not need the world. **The declaration IS the drawing**, so the harness runs it lit
into a blank canvas the size of the sprite and asks whether those pixels sit where the sprite's own
unlit bulb sits - with the bulb found by reading the BAKED SPRITE, never from a coordinate repeated
out of `road.js`:

| | x | y |
|---|---|---|
| the unlit bulb, read from the sprite | 0.845 - 0.882 | 0.679 - 0.738 |
| the lit lamp, from the declaration | 0.845 - 0.882 | 0.673 - 0.744 |

**And it bites.** Shifting only the LIT pass by 5% of the sprite - exactly what a reskin that moved
the art and left the glow behind would look like - moves it to `0.850 - 0.932` and the check fails.
A second assertion keeps the live path honest: asking for the indicator in the running game changes
pixels at that bulb.

**WHAT IS NOT DONE, and the ruling requires this to be written down.** Converted: the MATADOR tail
and its indicators. **Not converted: CREST, STALLION, FORMULA, the cruiser, the super cruiser, every
traffic body, the front sprites and the rear-view.** They still paint lamps straight into their
sprites with no declaration, and they are still lit by hand-placed geometry - `tailLights` for
traffic and the inline circles for an unconverted player body. `lamp-test` SKIPS rather than passes
when the chosen body is one of those, so a half-converted engine cannot read as a finished one.

**Still to do before [[RLG-052]] can be built:** traffic bodies must declare indicators, because that
is the vehicle that will actually signal. The seam is proved; the repetition is not.
- **RULED 2026-08-29T07:39:24-04:00** — **THE WHOLE FLEET IS CONVERTED, in v0.9.23, by the owner's instruction of 2026-08-29: "do the whole
fleet".** Part one proved the seam on one car; this repeats it across every vehicle the engine can
put on the road.

**84 lamps across 26 vehicles**, every one of them a single drawing run unlit into the sprite and
again lit on top of it. `tools/lamp-test.py` asserts it for all of them and `docs/fleet.png` shows
it, one row per vehicle: dark, braking, right, left, bar.

| painter | lamps declared |
|---|---|
| MATADOR, and the super cruiser that shares its tail | `tail` (two inner chevrons a side), `turn.l`/`turn.r` (the outermost chevron), `bar.rl`/`bar.rr` on the force car |
| STALLION | `tail` (the two inner rings), `turn.l`/`turn.r` (the outer rings) |
| CREST | `tail` (the full-width bar), `turn.l`/`turn.r` at the ends of the housing |
| FORMULA | `tail` only - the rain light |
| saloon, coupe, cab, roadster, tuner, cruiser | `tail`, `turn.l`/`turn.r` inboard of each cluster |
| muscle | `tail` (three bars a side), `turn.l`/`turn.r` outboard |
| pickup, van, lorry | `tail`, `turn.l`/`turn.r` below or beside the cluster |
| police cruiser | the above plus `bar.rl`/`bar.rr` |

**THE FORMULA CAR HAS NO INDICATORS. Owner's ruling, 2026-08-29:** *"The formula car does not need
turn indicators."* A single-seater has none, and that is what the car IS rather than something
unfinished. It is the ONE vehicle allowed to be missing them, `lamp-test` names it as the exception,
and naming it is what stops the exception spreading.

**Four corrections from the owner on the first sheet, all built:**

1. **The unlit bulb is a DARK amber and the lit one a BRIGHT amber** - not a near-black casing and a
   white core. It has to read as one lamp in two states, or a signal appears out of nowhere.
2. **On MATADOR the indicator IS the outermost chevron**, not an amber lamp bolted beside the
   cluster. A tail that reads as one design in the dark should read as one design lit.
3. **On STALLION the indicators are the outer two of the four rings.** Four rings is the car's
   signature and hanging a fifth shape off each end spoils it - and a real four-ring tail does
   exactly this.
4. **The sheet showed the fleet twice.** The garage SALOON and the traffic sedan come out of one
   painter with different paint. Rows are grouped by painter now and each says who drives it.

**`tools/lamp-test.py` checks the fleet without knowing a single colour**, and the first version of
that check was worthless. It compared BOUNDING BOXES of the lamp lit and unlit - and a box cannot
see one lamp of a pair sliding inside it. Deliberately shifting a saloon's left tail lamp by 3% of
the car went undetected. It compares PIXELS now, with one pixel of slack for antialiasing and a 2%
tolerance, and the same drift fails it at 50 stray pixels of 1,156.

**Still not converted, and this is now a short list:** the FRONT sprites (headlights) and the
rear-view. Every rear sprite in the game is done. The front is a separate painter with its own lamp
art and is the obvious next piece if headlights are ever to be driven by anything.
- **RULED 2026-08-29T07:43:18-04:00** — **FOUR MORE CORRECTIONS FROM THE OWNER on the second sheet, 2026-08-29, all built in v0.9.24.**

1. **STALLION: all four rings are brake lights, and the indicator is the DOT INSIDE the outer two.**
   The first attempt made the outer rings themselves amber, which was a misreading - it changed what
   the brake light IS. A four-ring tail carries its indicator in the middle of the outer pair, and
   doing it that way leaves the car's signature intact lit or dark.
2. **MUSCLE: four boxes a side, and the outermost box is the indicator.** It was three bars with an
   amber lamp beside them. Same idea as MATADOR's outermost chevron - the signal is part of the
   cluster rather than a shape stuck next to it.
3. **VAN: the indicator goes ABOVE the brake lights, and only there.** A van's corner cluster stacks
   upward.
4. **LORRY: the roof running lights illuminate as brake lights as well.** They are part of the tail
   declaration now rather than decoration painted once - which is what a lorry does, and it is the
   thing you actually see of one at night when it slows in front of you.

**And the duplicate rows the owner asked about were mine, not the engine's.** The sheet shown in
chat had been rendered BEFORE the grouping fix landed; the file written to disk was the grouped one.
The sheet now prints each row's painter signature - `rig:cop`, `car:SUPERCRUISER` - so two rows that
look alike in white can be told apart at a glance rather than read as a duplicate. **When a picture
is the deliverable, the version shown and the version saved have to be the same render**, and they
were not.

**The fleet is 15 distinct vehicles**, not 26: the garage SALOON and the traffic sedan are one
painter in two liveries, and eleven such pairs exist. The 26 figure is sprites, and it is the one
`lamp-test` counts.
- **RULED 2026-08-29T07:52:07-04:00** — **TWO MORE, v0.9.25, and both were caused by not looking closely enough.**

**The van had TWO ambers.** Its cluster has always carried an amber band across the top of each red
lamp - it was in the art before any of this - and the conversion added a bulb ABOVE that. The owner:
*"The indicators on the van are weird. We only need a single indicator above each brake light."*

**The band IS that single indicator**, and it is declared now rather than replaced. That is the
ruling working in the direction it was meant to: the art was already right and nothing was reading
it. The added bulb is gone.

**The lorry's roof row lit PALE YELLOW when it was asked to light red**, and the reason is worth
keeping. A lit lamp is composited with `lighter`, which ADDS. The row was painted amber, and
amber plus red is yellow. **A lamp that lights red has to be a red lamp when it is dark** - so the
roof row is dark red now and lights bright red, which is also what a lorry's REAR roof markers are.
The amber ones are on the front.

**The general trap, and it applies to every lamp added from here:** under additive compositing the
unlit colour is not covered up, it is part of the result. A declaration whose two states are in
different hues will land on a third hue that neither of them is.

**And the method note.** Both faults were invisible in the fleet sheet at one row per vehicle and
obvious the moment the van and the lorry were rendered three times the size, side by side, dark and
lit. `tools/fleet-sheet.py` is the overview; when a specific vehicle is in question, render THAT
vehicle large before changing anything. The first attempt at the van fix - making the added bulb
flat instead of two-part - was aimed at the wrong lamp entirely, and a zoom would have said so
before the change rather than after.
- **RULED 2026-08-29T07:55:33-04:00** — **v0.9.26, three more from the owner and one of them was a repeat.**

**THE VAN, third time, and the instruction was exact:** *"a single brake light and above it a single
indicator lamp. One of each on each side of the vehicle."* What was there was one TALL red lamp with
an amber band painted across its top - one object that read as two - and the previous two attempts
had each adjusted the wrong part of it. It is now two separate lamps with air between them: amber
above, red below, one of each per corner. **Both earlier attempts changed a lamp without rendering
that vehicle large first, and both were wrong.**

**THE LATERAL CLUSTERS: the indicator goes OUTBOARD.** Owner: on roadster, tuner, cruiser, coupe,
saloon and cab the indicator belongs on the outside rather than the inside. Inboard put the two
ambers close together in the middle of the car, where they read as one central lamp rather than as a
SIDE being signalled - which is the entire job of an indicator. It is carved out of the cluster
rather than added beside it, so the tail is the width it always was and the amber is its outermost
segment. That is now the pattern on every vehicle that has room for it: MATADOR's outermost chevron,
MUSCLE's outermost box, and these.

**AND THERE IS NO "GARAGE" VERSION OF ANYTHING.** The sheet labelled rows `garage VAN / also traffic
van`, which is an implementation detail leaking into a picture: the garage car and the traffic car
are ONE VEHICLE, and the sheet says its name once. `API.fleet` names them plainly now.

**The method, stated once because it has cost three rounds:** the fleet sheet is the overview and it
is not enough to judge a single vehicle by. When one vehicle is in question, render THAT vehicle at
three times the size, dark and lit, and look at it BEFORE changing anything. Every wrong fix in this
sequence was made from the overview; every right one was made from the zoom.
- **RULED 2026-08-29T08:22:36-04:00** — **THE FRONTS, v0.9.27, and with them the last of the vehicle sprites.**

Every front in the game declares its lamps: `head`, `turn.l`, `turn.r`, and `bar.fl`/`bar.fr` on the
two force cars. **84 lamps became 130 across the same 26 vehicles.**

**And it fixed something nobody had noticed: every car's headlights were on at noon.** `headGlow`
painted a bloom straight into the sprite, so a parked car glowed in the garage and every car on the
road burned its lamps in daylight. A headlight is a lamp like any other now - a dim lens when it is
off, the lens and its bloom when something asks.

**WIPERS.** A wiper is not a lamp: it has no on and off, it has a POSITION. So the sprite carries
`wipers(g, t)` alongside its lamps - 0 parked, 1 at full sweep - and bakes the parked pose. The
animation is the same drawing at a different argument rather than a second description of a blade.
Both front painters derive theirs from the screen rectangle they just drew, so a car with a different
screen gets different wipers without anything in the wiper code changing.

**THE MUSCLE CAR loses its forced stripes and gains a blower.** Owner, 2026-08-29. It wore stripes
whether or not the option was set, which made the option a lie on the car most likely to want it.
What says MUSCLE now is an exposed blower standing out of the bonnet and cutting into the bottom of
the windscreen - drawn after the screen and the wipers so it stands in front of them, because a car
you cannot quite see the driver of is a different car. Its outer headlight is a bigger bulb than its
inner one, which is what a quad-lamp car does and what stops four identical dots reading as a grille.

**FIVE CORRECTIONS ON THE FRONTS, all the owner's, and two of them were my mistakes rather than
choices:**

1. **Every supercar wore MATADOR's face.** `paintFront` takes its body under `bodyType` - `o.body` is
   a COLOUR on every other painter and the comment inside says so - and the fleet accessor passed
   `kind`. Five cars, one nose, and the sheet showed it plainly.
2. **A headlight is not an indicator.** I had made the outboard lens of MATADOR's pair amber. The
   owner: *"you substituted one of the matador's headlights into the turn indicator - I don't want
   that."* **That is the difference between the two ends of a car:** at the REAR a cluster is a row
   of repeated elements and taking the outermost reads as design; at the FRONT there are two lamps
   and taking one removes a headlight. Front indicators are their own lamp now.
3. **STALLION and MATADOR stack.** The indicator runs along the BOTTOM of the headlight at the same
   rake, so the two read as one unit. On the MATADOR that is free - the housing is already a rotated
   frame, so a bar drawn under it in that frame is at the lamp's angle by construction.
4. **CREST is the exception, and it proves the rule.** Its face is a BANDED bar - repeated segments -
   so the owner ruled the outermost segment IS the indicator. That is the rear's answer, and it works
   here for the rear's reason.
5. **The muscle car's ambers were off the bodywork**, floating beside it at 0.010 and 0.952.

**THE SHEET IS FIVE SHEETS.** One picture of everything was unreadable at any size that fits on a
screen. `docs/fleet-super.png`, `-sport`, `-police`, `-production`, `-utility`, each with cells nearly
three times the area, two rows per vehicle - back then front - and the steering wheels at the end.
**A class is the grouping the fleet actually has**, not one invented for the page, so `API.fleet`
carries it and the sheet only reads it.

**The steering wheels needed a way in.** `drawWheel` paints the HUD canvas and refuses outright on a
device with no touch, which is every harness. The refusal is lifted for the duration of
`API.wheelOf` and the result copied off - and that first attempt silently produced four blank cells,
because the patch that lifts it had been skipped by a shell `&&` after an earlier command failed.
**A picture that renders without error is not a picture that rendered.**
- **RULED 2026-08-29T08:41:45-04:00** — **v0.9.28, and the colour rules the owner asked me to verify are INTACT. Checked, not assumed:**

| rule | where it lives | state |
|---|---|---|
| a cab is always yellow | `paintChoices()` returns `['GOLD']` for CAB | intact |
| the cruiser and super cruiser are black-and-white only | `paintChoices()` returns `['WHITE','BLACK']`, and `liveryBand` inverts the band so it contrasts either way | intact |
| the lorry's box is not the chosen colour; the cab is | `buildSprites` splits the paint, cab under `P.cab` | intact, and now CHANGED by ruling |

**THE LORRY'S TRAILER TAKES THE COLOUR, MUCH DARKER.** Owner, 2026-08-29, offering the change
himself: instead of the fixed beige, a much darker shade of the chosen paint. That keeps what the
old rule was protecting - the cab is still what wears the colour and the box is still the quiet half
- while letting a lorry you painted actually look painted. `shade(..., 0.34)`, which is dark enough
that WHITE reads as a grey box rather than a second white cab.

**And it exposed a fault the rear view had hidden.** The FRONT painter drew the cab from `P`, which
on this rig is the TRAILER's shade - so the whole face came out the colour of the box. The rear
never needed `P.cab` because from behind a lorry IS its box; from the front it is the cab you see.

**A HINT OF THE TRAILER, ABOVE THE CAB.** Owner: seen head on, a lorry is a cab with a box standing
behind and above it, and without that the front view was a tall van. A band of the trailer's colour
across the top, wider than the cab, is all it takes.

**THE WIPERS ARE NOT BAKED INTO THE SPRITE.** They were drawn parked into the canvas AND handed back
for animation, so anything that swept them painted a second pair over the first - **both poses at
once, which is what the owner saw in the last cell of the sheet.** A wiper is a moving part: it is
registered by the painter and drawn by whoever draws the car, at whatever point in its sweep. Parked
is simply `t = 0`, and the mirror draws it every frame.

**THE BLOWER, twice.** First it floated - raising it to cut the screen left the case where it was,
with air between it and the bonnet. It runs down to `pDeck` now, the bonnet line the painter already
uses, so it is anchored to the car rather than to a number chosen beside it and cannot float again
if the height changes.

Then the owner asked for a better design, and the first was a slab with a stripe. **What makes a
blower read as one is three things stacked, and none of them is the box:** a SCOOP that flares wider
than everything under it with its mouth facing you and velocity stacks inside; a CASE below that is
narrower and ribbed across; and the DRIVE PULLEY at the bottom where the belt comes off. Drawn
bottom-up so each part overlaps the one behind it, which is what gives it depth without a gradient
doing the work.
- **RULED 2026-08-29T08:43:52-04:00** — **THE BLOWER TOOK THREE ATTEMPTS AND A PHOTOGRAPH.** Worth recording because the failure was the
same one twice, and the fix was not a better version of it.

1. **A slab with a stripe.** Read as a lump.
2. **A wide flared scoop with a mouth cut in it**, spanning most of the cabin. That is a HOOD SCOOP,
   not a blower, and being wide is what made it read as bodywork rather than as an engine.
3. The owner sent a photograph. What is actually on a car like that:

   - **three round TRUMPETS** in a row, standing proud and open to the sky, with coloured mouths -
     red in the reference;
   - a small **INJECTOR HAT** under them, the width of the three;
   - a polished **CASE** below that, ribbed across;
   - the **DRIVE PULLEY** at the bottom where the belt runs;
   - and the whole thing **NARROW** - about a fifth of the car's width - coming up through a hole in
     the bonnet rather than lying on it.

**The lesson is about the reference rather than the drawing.** Twice I made something with the right
parts in the wrong proportion, and no amount of adjusting the wide version would have arrived at the
narrow one - the shape was wrong, not the detail. **Ask for the picture first when the thing being
drawn is a real object somebody knows the look of.**
- **RULED 2026-08-29T09:01:04-04:00** — The three utility vehicles had no wipers. Every other car on the road carried a pair from the day the
part was added, and the pickup, the van and the lorry did not - the vehicles that would need them
most.

The cause is a shape in the front painter rather than an oversight of art. `paintRigFront` gives each
utility body its own branch, because a lorry's face is mostly glass and a pickup's is a cab set back
behind a bed. Each branch draws its own screen and RETURNS. The shared registration that hands the
wipers back sits at the bottom of the painter, after all three returns, so it never ran for any of
them. Nothing failed and nothing was reported: the parts registry simply held no wipers, and the
sheet drew the same picture in the parked cell and the swept one.

Each branch now registers its own pair, from the screen it has just drawn. The lorry's takes the
CAB's paint through `P.cab`, not the trailer's - an arm the colour of the box would belong to the
wrong half of the vehicle.

The general lesson is about early returns in a painter. A function that ends in shared work, and that
some bodies leave before reaching it, silently omits that work for exactly those bodies. The wipers
were visible on the sheet, which is how this was found at all; anything registered the same way and
never drawn would still be missing.
- **RULED 2026-08-29T09:07:18-04:00** — The sheet built every body in WHITE. That is correct for the ten cars that come in a dozen colours
and wrong for the three that do not: the cab came out white, and a cab is yellow - it is the whole of
how a person recognises one - and both patrol cars came out white when the force runs a
black-and-white and a white-on-black.

`API.fleet` now names the paint each body is allowed to wear, which is the restriction
`paintChoices()` already puts on the garage. A body with two liveries produces TWO ROWS, because a
reader groups by `sig` and would otherwise keep the first and drop the second. The force takes
`COP_PAINT` rather than the garage palette: those are the two paints an on-duty cruiser is actually
built with. Everything else stays white, by the owner's instruction - a sheet in a dozen colours
tells a reader about paint rather than about lamps.

That split `name` from `key`. The name is what a reader prints, CRUISER . BLACK; the key is the body
it was built from, CRUISER, and it is what the class table and the steering wheel are looked up with.
Without it a livery row fell to the default class and drew no wheel.

Each row also gains a STRIPES frame. Stripes are paint rather than a body, so any car can wear them -
except a car already wearing a livery of its own, which is the formula car and both cruisers. The
cell is empty for those three, and an empty cell is the answer rather than a missing frame.
`stripesAllowed()` could not be called for this: it reads `optBody`, so it cannot answer for a car it
is not looking at.
- **RULED 2026-08-29T09:09:37-04:00** — Owner, 2026-08-29: a production car does not take stripes, and neither does a police car or a utility
one. The old rule was an EXCLUSION of two vehicles - the formula car and the cruiser, both of which
already wear a livery of their own - which left every delivery van in the game free to wear a pair of
Le Mans stripes over its roof.

A racing stripe is a claim about what a car is for, and a saloon, a cab, a van and a patrol car are
not making it. The rule is a LIST now rather than an exclusion, and the list is the two classes a
stripe belongs to: the supercars and the sports cars. The formula car stays out although it is a
supercar, because its livery is the whole of its bodywork.

`stripesAllowed()` read `optBody`, so it could only answer for the car the player was looking at. It
is now `stripesOn(k)` keyed by body, with `stripesAllowed()` as the one-argument case. The garage and
the fleet sheet ask the same function, so a body added later is out of the list until somebody puts
it in - which is the safe direction for a rule about what a car is allowed to claim about itself.

This is a change to the GAME and not only to the sheet: the STRIPES button no longer appears in the
garage for the six production and utility cars.
- **RULED 2026-08-29T09:16:21-04:00** — Owner, 2026-08-29: the production and utility cars want a less sporty steering wheel.

Every road car was being handed the sports wheel with its flat bottom rounded off - which is a
supercar's wheel with one feature removed rather than a different wheel. It still carried carbon
weave across the top third, chrome inserts down the spokes, a silver bezel around switchgear, and the
racing tick at twelve o'clock that tells a driver where straight-ahead is.

None of that is on the wheel of a van, so the plain wheel is now its own object rather than a
subtraction from the sporty one. It takes: a moulded rim with a flatter gradient, because plastic
does not catch light along its length the way leather does; no weave; no twelve-o'clock stripe; plain
moulded arms with a soft top edge instead of bezelled switch panels; a spoke with no chrome insert;
and a WIDE HORN PAD in place of the small machined hub, which is the other half of why the two read
differently at a glance. The badge still sits on the pad.

The lorry takes a thinner rim than the rest, 7.4 against 8.6. A lorry wheel is large and thin because
it is turned with the whole arm rather than gripped, and a fat sports rim on one reads as the wrong
vehicle.

Keyed by BODY rather than by marque. The marque of every production and utility car is GENERIC, which
cannot separate them from the traffic that shares it, and `optBody` is what `API.wheelOf` sets. The
patrol cars are deliberately not in the list: they are pursuit vehicles, and the owner named
production and utility.
- **RULED 2026-08-29T11:52:09-04:00** — Owner, 2026-08-29, once the two ends were finally hung from the same line: "make sure that the front
matches the same curvature as the back. Look at the top rounded edges."

They did not match, and the comment above the code claimed they did. The rear body is a PATH with a
waist in it: the deck line runs from 0.115 to 0.885, the shoulders curve outward to 0.955 and 0.045,
and the sill tucks back in at the bottom. The front was a rounded rectangle from 0.055 to 0.945 with
a 0.035 corner - a slab with the corners taken off - under a comment reading "0.055 to 0.945, exactly
as the back".

It was exactly as the back at the two EDGES and a different shape everywhere between them. That is
the most durable kind of wrong comment: it was true of the numbers it named.

The front now draws the rear's own path with `pDeck` where the rear uses `deckY`, and its arch
blisters are the rear's ellipses at the rear's 0.135 and 0.865 rather than flat ovals at 0.13 and
0.87. Both ends of a car are one silhouette.

The vehicles with their own branches - the pickup, the van and the lorry - were checked pair by pair
on the class sheets and are consistent: a pickup's bed and a lorry's trailer are genuinely different
objects from their cabs, and that difference is the vehicle rather than the drawing.
