+++
id                = "UNT-154"
kind              = "unit"
status            = "closed"
cost              = 3051
closed            = "2026-09-01T21:23:01-04:00"
created           = "2026-09-01T21:18:33-04:00"
created_precision = "second"
source            = "owner, 2026-09-01"
supersedes        = []
blocks            = []
caused_by         = ["RLG-153"]
see_also          = []
delegate          = false
effort            = "high"
model             = "claude-opus-5"
ran_on            = "claude-opus-5"
routing           = "defect"
+++

### UNT-154 - An event keeps its exit ramp, and its shape is handed over rather than switched on

- Routing class: `defect` -> **claude-opus-5**, effort **high**. Bias 1. Finding the cause is the work, and a wrong guess costs a second pass. ([[RLG-092]])
- Opened for: [[RLG-153]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-09-01T21:23:01-04:00** - An event keeps its exit ramp, and its shape is handed over rather than switched on. RLG-153 item 2 and RLG-148, measured before anything was touched as that fragment required. The formula was never wrong: the NEXT place armed its event over the top of the one still in force, because a boundary is placed at the horizon a draw distance before the car reaches it, and an ordinary place has no profile so it cleared the bore's. Every event lost its last 30,000 units - a tunnel's slope 86 per cent through read -0.09 where the profile asks for +0.34. A new event waits for the car to drive out of the old one; they are adjacent rather than overlapping so nothing is lost by waiting. And the profile is MIXED with the terrain over the first and last 4 per cent instead of replacing it outright, because a replacement thrown 30,000 units before the mouth re-shaped every visible slice in one frame. The debug setters arm outright, since they stand in for the driving as well as the placement. event-test drives a timer-placed passage now: everything it claimed before was about a profile the harness installed and nothing ever disturbed, which is why a green run said nothing.
