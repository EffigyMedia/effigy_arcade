+++
id                = "EVD-236"
kind              = "evidence"
status            = "recorded"
step              = "the water is on both sides, fills the row under the skyline edge to edge, recedes, and reaches the mirror - and every one of those goes red on a build without it"
unit              = "UNT-137"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/bridge-test.py"
exit_code         = 0
duration_ms       = 10780
output_bytes      = 1826
output_sha        = "142a81256e704c9b3d876e3634e0c732"
store_stamp       = "e5e765d73dd9cfcf"
created           = "2026-09-01T09:38:10-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-236 — step `the water is on both sides, fills the row under the skyline edge to edge, recedes, and reaches the mirror - and every one of those goes red on a build without it` of [[UNT-137]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/bridge-test.py`
- Result: **exit 0**, in 10780 ms
- Output: 1826 bytes, sha256 `142a81256e704c9b3d876e3634e0c732`
- Store stamp at the time: `e5e765d73dd9cfcf`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
bridge-test  .  water on both sides, to the horizon, receding
  browser: playwright bundled chromium (pinned)

  WHICH PLACES ARE OVER WATER
      COASTAL    sea=True  overWater=False
      BRIDGE     sea=True  overWater=True
  ok    the bridge states that it is over water
  ok    and the coast does not - it has a shoreline, which is the difference
  ok    and nothing else on the board is over water

  WATER ON BOTH SIDES, WHICH IS WHAT A COAST NEVER HAS
      bridge, one row a quarter down from the horizon:  left 26  right 26  of 480
      coast,  the same row:                              left 0  right 0  of 480
  ok    the bridge has water to left AND right of the road
  ok    and the coast still has land on one side, so this measures a bridge

  AND IT REACHES THE HORIZON, WHERE THE ROAD PASS STOPS PAINTING
      the far field: drew=False, horizon 345.0, the road stops at 357.6
  ok    the far field knows the place it is painting has water in it
      the row just under the skyline, which only the far field paints:
        bridge  480 of 480 pixels are water
        coast   196 of 480
  ok    the bridge fills that row edge to edge, so the water reaches the skyline
      the band as a whole: 9 rows, water left 2159 right 2156 of 4320 - reported, not asserted on, see above

  AND IT RECEDES, OR IT IS A PAINTED FLOOR
      near  rgb(34,77,99) from 18 pixels
      far   rgb(47,77,93) from 480 pixels
  ok    the water at the skyline is not the water at the bumper

  AND THE GLASS CARRIES IT TOO
      mirror water pixels: left 696  right 713  of 6750
  ok    the mirror shows water on both sides as well
  ok    no page errors

all checks passed
  whether it reads as a BRIDGE rather than a causeway is not measured here -
  it will not until the towers and the deck are built. See tools/biome-shot.py
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
