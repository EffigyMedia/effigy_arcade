+++
id                = "EVD-004"
kind              = "evidence"
status            = "recorded"
step              = "the tow is inert and the governor is not, measured against a band-off control"
unit              = "UNT-001"
command           = "python Projects/In-Dev/Effigy_Arcade/tools/band-test.py --repeats 3"
exit_code         = 0
duration_ms       = 203808
output_bytes      = 1689
output_sha        = "31b19507249cd5d3e571d0040f4374a4"
store_stamp       = "97ade5813a5d6e6f"
created           = "2026-08-28T16:52:35-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-004 — step `the tow is inert and the governor is not, measured against a band-off control` of [[UNT-001]]

- Ran: `python Projects/In-Dev/Effigy_Arcade/tools/band-test.py --repeats 3`
- Result: **exit 0**, in 203808 ms
- Output: 1689 bytes, sha256 `31b19507249cd5d3e571d0040f4374a4`
- Store stamp at the time: `97ade5813a5d6e6f`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
band-test  -  RLG-029  -  25s per arm after a 6s settle
  MILE 128,700 units  -  MAX_SPD 15,333 (200mph)  -  AI_TOP 13,800 (180mph)
  browser: playwright bundled chromium (pinned)
  pair 1 of 3 done
  pair 2 of 3 done
  pair 3 of 3 done

  BAND ON   (mean of 3 runs)
    player averaged 99.4 mph
    tag       samples   lead(mi)       mph   spd/base    at cap   headroom
    behind        749      -1.99     179.0      0.951       92%      -4.4%
    near          729      -0.10      98.1      0.516        4%      -5.3%
    ahead         749      +2.00     154.0      0.803        0%      -6.1%

  BAND OFF   (mean of 3 runs)
    player averaged 104.2 mph
    tag       samples   lead(mi)       mph   spd/base    at cap   headroom
    behind        748      -1.99     178.7      0.949       92%      -4.4%
    near          708      -0.10     110.8      0.583        6%      -5.3%
    ahead         748      +2.00     167.0      0.871       74%      -6.1%

  THE ANSWER  -  what the band changes, band on minus band off
    tag             mean   per-run spread
    behind        +0.17%   -0.02%, +0.57%, -0.04%
    near          -8.91%   -32.67%, +28.30%, -22.36%
    ahead         -7.78%   -7.82%, -7.76%, -7.77%

  The band claims up to 14 percent either way. A difference near zero means it is
  inert, and the control arm above proves the measurement can see a difference.

  HEADROOM is what the tow has to clear. `want` is capped at AI_TOP on the line
  AFTER the band multiplies it, so a rival whose base already exceeds AI_TOP has a
  NEGATIVE headroom and no tow can reach it at all. The governor is unaffected: it
  pulls `want` down, and the cap only ever pulls down as well.
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
