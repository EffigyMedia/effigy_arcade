+++
id                = "UNT-089"
kind              = "unit"
status            = "closed"
cost              = 23780
closed            = "2026-08-31T00:18:25-04:00"
created           = "2026-08-31T00:08:13-04:00"
created_precision = "second"
source            = "owner report, 2026-08-31"
supersedes        = []
blocks            = []
caused_by         = ["RLG-090"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-089 - The world settles before the count, not on GO

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-090]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-31T00:18:25-04:00** - The weather and the cloud roll at the reset instead of on the first frame after GO. A counter left at zero means a roll is DUE, and the count-in returns from step() before the world runs, so both were deferred to GO and happened in front of the player. The biome had this fault and was fixed one version earlier; these are the same shape twice more, in the two lines beside it. The rolls are lifted out of their steppers so the reset and the frame update share the code, the run starts AT what it rolled rather than easing toward it, and both counters are armed to a full interval. THE CHECK ASKS THE COUNTERS RATHER THAN THE PICTURE: comparing the world during the count with the world after GO only catches this when the deferred roll produces weather, and it came up dry one run in three and passed on a broken build; asking whether anything is due while the car is held fails three in three. AND retry-test WAS RESTATED, not kept - it asserted a fresh run was dry, which was an accident of the old implementation, and a run that rolls its own weather may legitimately start in snow. It now asks what a new run actually means. THE OWNER NAMED THE FIX BEFORE I FOUND IT.
