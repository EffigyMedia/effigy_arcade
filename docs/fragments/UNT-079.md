+++
id                = "UNT-079"
kind              = "unit"
status            = "closed"
cost              = 20825
closed            = "2026-08-30T20:14:20-04:00"
created           = "2026-08-30T20:07:41-04:00"
created_precision = "second"
source            = "owner report, 2026-08-30"
supersedes        = []
blocks            = []
caused_by         = ["RLG-087"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-079 - The garage car sits in one box, sized to the tallest car

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-087]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-30T20:14:20-04:00** - The garage card is one height for every car - the tallest card any body produces, computed by loading each in turn and putting the player's own car back, cached after the first time. It moved 78, 85, 90 across the starting cars before. This REVERSES a deliberate decision: the height was made per-car to avoid dead space under a low car, and the owner has now weighed the two with the thing in front of them. THE LORRY IS AN OUTLIER at 150 against 115 for the next tallest, so a roadster now sits above 72 pixels of air; the lever is GARAGE_DEEP, which only the lorry reaches. The check asserts the reservation covers the tallest AND is exactly that car with nothing spare, then switches cars four times and requires one height; with the per-car height put back it reads 78, 85, 90, 78.
