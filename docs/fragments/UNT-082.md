+++
id                = "UNT-082"
kind              = "unit"
status            = "closed"
cost              = 25420
closed            = "2026-08-30T20:43:02-04:00"
created           = "2026-08-30T20:32:44-04:00"
created_precision = "second"
source            = "owner report, 2026-08-30"
supersedes        = []
blocks            = []
caused_by         = ["RLG-089"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-082 - A race has checkpoints, whatever the timed toggle says

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-089]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-30T20:43:02-04:00** - A race places checkpoint boards with no condition at all: being inside clockRuns() IS the condition, and TIMED is a choice about a test drive rather than something a race consults. The old line asked timedRun again inside clockRuns() and got a different answer, and it consumed each checkpoint anyway, so turning the toggle back on mid-run could not recover them. THE OWNER'S OWN SEQUENCE PASSES ON THE BROKEN BUILD - it leaves TIMED on - so race-test carries both the reported sequence and the discriminating one, TIMED left off, which reads 0 boards on the old code and 1 on the fix. The harness also had to drive 18 seconds rather than 4: a checkpoint is two miles out and the placing loop looks 90,000 units ahead, so the first version found its own arithmetic rather than the defect. ALSO IN THIS COMMIT, at the owner's request: the countdown was mis-numbered 0.11.0 and is renumbered 0.10.9, since one feature is one patch; this release is 0.10.10 because version numbers only climb.
