+++
id                = "EVD-226"
kind              = "evidence"
status            = "recorded"
step              = "the canyon horizon has no gaps, does not jump, and closes on itself - and no other place on the board passes all three"
unit              = "UNT-134"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/canyon-test.py"
exit_code         = 0
duration_ms       = 6845
output_bytes      = 2556
output_sha        = "ebe35bbacb8ffcac22b57c7355794333"
store_stamp       = "8ae8a8d7e8a06c04"
created           = "2026-09-01T08:36:05-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-226 — step `the canyon horizon has no gaps, does not jump, and closes on itself - and no other place on the board passes all three` of [[UNT-134]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/canyon-test.py`
- Result: **exit 0**, in 6845 ms
- Output: 2556 bytes, sha256 `ebe35bbacb8ffcac22b57c7355794333`
- Store stamp at the time: `8ae8a8d7e8a06c04`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
canyon-test  .  the horizon is one mass, and it closes on itself
  browser: playwright bundled chromium (pinned)
  ok    the canyon is on the board
  ok    and it states a continuous form rather than a row of objects

  THE SILHOUETTE OF EVERY PLACE, BY THE SAME THREE RULES
      place      form       gaps   step   seam
      COASTAL    open        582      3      6
      FARMLAND   open        427      5      0
      SWAMP      wetTree      27      9      8
      FOREST     tree          1     14     65
      DESERT     mesa         60      7     59
      MOUNTAIN   peak          0      4     14
      CITY       tower         2    122     26
      TUNDRA     peak          0      3      9
      CANYON     ridge         0      1      0

  THE CANYON, AGAINST WHAT A CONTINUOUS FORM HAS TO BE
  ok    the canyon has a silhouette to read
  ok    no column of the wall reaches the ground - it has no gaps in it
  ok    and the crest walks rather than jumping, so it is not a comb
  ok    and it closes on itself, so the tiled seam is not a cliff

  AND THE SAME RULES ON EVERY OTHER PLACE, WHICH IS THE FALSIFICATION
      COASTAL    breaks gaps,seam
      FARMLAND   breaks gaps,step
      SWAMP      breaks gaps,step,seam
      FOREST     breaks gaps,step,seam
      DESERT     breaks gaps,step,seam
      MOUNTAIN   breaks step,seam
      CITY       breaks gaps,step,seam
      TUNDRA     breaks seam
  ok    no free-standing horizon passes all three, so the rules measure something
  ok    and the ones that are already gapless fail on the seam, which is the walk

  THE WALL IS NOT DRAWN AT HORIZON SCALE
      {'COASTAL': 1, 'FARMLAND': 1, 'TUNNEL': 1, 'SWAMP': 1, 'FOREST': 1, 'DESERT': 1, 'MOUNTAIN': 1, 'CITY': 1, 'TUNDRA': 1, 'CANYON': 2.4}
  ok    the canyon states a taller band than an ordinary horizon
  ok    and nothing else does, so the ordinary horizon is unchanged

... 14 more line(s), covered by the digest
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
