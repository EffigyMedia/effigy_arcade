+++
id                = "UNT-077"
kind              = "unit"
status            = "closed"
cost              = 14281
closed            = "2026-08-30T19:50:50-04:00"
created           = "2026-08-30T19:44:22-04:00"
created_precision = "second"
source            = "owner report, 2026-08-30"
supersedes        = []
blocks            = []
caused_by         = ["RLG-086"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-077 - The garage rebuilds one car, not the whole road

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-086]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-30T19:50:49-04:00** - The garage rebuilds the player's car instead of every sprite in the game. Timed first: the click handler was 215 to 335 ms and the sprite build alone was 219 to 240 ms of it, so the owner's reading that it was rebuilding the vehicle was right - it was rebuilding all of them, including the whole rival cache and every traffic sprite, none of which read the colour, the stripes or the chosen body. Split into buildPlayer and buildFleet at a seam that was already there; buildSprites still calls both, and only the three paths a thumb reaches in the garage were narrowed. After: a colour tap 3.9 to 6.2 ms, the stripes toggle 3.8 to 4.2 ms, and the full build unchanged at about 215 ms as the control. The check is not a stopwatch - a sprite is a canvas, so title-test marks the lorry's sprite and the player's, taps a colour, and asserts only the player's was rebuilt. Put the full rebuild back and it fails.
