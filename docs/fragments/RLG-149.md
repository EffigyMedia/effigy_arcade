+++
id                = "RLG-149"
kind              = "ruling"
type              = "perf"
status            = "requested"
created           = "2026-09-01T17:17:03-04:00"
created_precision = "second"
source            = "owner, 2026-09-01"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = ["RLG-002"]
+++

### RLG-149 — An older iPad cannot hold the frame rate, and the owner asks whether Godot with rasterized art is the answer
- **RULED 2026-09-01T17:17:46-04:00** — Owner, 2026-09-01, from two devices: "this game runs phenomenal on my iPhone 17 Pro Max but
when my daughter tried playing it on her much older iPad, it has like incredibly bad frame rate
issues. I'm wondering if it can be made much more performant if it gets translated to Godot
script and into Godot where we rasterize all the art."

THIS IS THE FIRST REPORT FROM A SECOND DEVICE AND THAT IS WHAT MAKES IT VALUABLE. Every frame
rate this project has ever measured came off one phone and a desktop browser, so a floor has
never been known. A device that cannot hold the frame rate is a measurement nobody had.

THE QUESTION HAS TWO HALVES AND THEY HAVE DIFFERENT ANSWERS.

    WOULD GODOT WITH RASTERIZED ART BE FASTER? Almost certainly yes, and that route is already
    the plan for each machine ([[RLG-002]]). Godot draws through the GPU; this draws through
    Canvas 2D, which on an older iPad is largely a CPU rasteriser. Turning the vector work
    into textures moves it from per-frame arithmetic to per-frame blitting.

    BUT IT IS NOT THE FIRST THING TO TRY, because the conversion is a rewrite and the cause
    has not been named. A rewrite that carries the same overdraw across gets a faster machine
    doing the same wasteful thing. And the codebase is meant to be the playable design
    document that the Godot build is made from - so the cheap wins are worth having whichever
    route is taken, since the tuning survives and the plumbing does not.

WHAT THE SUSPECTS ARE, IN THE ORDER THEY SHOULD BE MEASURED. All three are fill rate rather
than logic, which is what a device-dependent frame rate usually means: the same JavaScript runs
on both machines, and only the pixel throughput differs.

    OVERDRAW IN THE ROAD PASS, WHICH IS THE LARGEST BY FAR. `drawRoad` walks about 150 slices
    far to near, and each one fills the ground FULL WIDTH from its own line down to the bottom
    of the screen. That is deliberate - the note in the function explains that it is what
    builds the terrain silhouette over a brow - and it means the screen is painted something
    like a hundred times per frame before anything else is drawn. On a phone with the fill
    rate to spare that is free. On an older tablet it is the whole budget.

    GRADIENTS BUILT EVERY FRAME. `createLinearGradient` and `createRadialGradient` are called
    inside the draw, not cached: the bore builds one per wall and one per vault, the vignette
    and the damage wash build one each, and every lamp pool builds its own. A gradient object
    is not free to construct and none of them changes between frames of the same place.

    THE SCENERY IS ALREADY CACHED AND IS PROBABLY NOT THE FAULT. `sprite()` renders to an
    offscreen canvas and the plans are cached per place, so trees and buildings are blits
    already. That is worth knowing before anyone spends a day on them.

HOW TO ANSWER IT WITHOUT GUESSING. `tools/fps-test.py` already measures frames per biome and
already carries the warning that a single run is noise - it takes several samples and prints
the range. What it does NOT do is throttle, so it cannot stand in for the iPad. Two things are
needed: a CPU-throttled run so the desktop can act as the slow device, and a per-pass cost
breakdown so overdraw can be separated from everything else. Neither exists yet.

AND THE OWNER SHOULD BE ASKED WHICH iPAD IT IS. The answer changes what "much older" means by
a factor of several, and it decides whether this is a floor worth designing to or one device
below the line.

WHAT IS NOT IN SCOPE. [[RLG-002]] says not to invest in work whose only value is on the web. A
fill-rate fix is not that - it is the same overdraw in Godot - but a bundle-size or
cache-behaviour fight would be.
- **RULED 2026-09-01T17:18:14-04:00** — AND THE OWNER NAMES WHAT THE WEB BUILD IS ACTUALLY FOR, WHICH CHANGES THE ANSWER ABOVE.
Owner, 2026-09-01, immediately after the report: "the thing I like best about it being a bunch
of HTML pages is that we push it to the GitHub and then I'm instantly able to play it on my
phone whereas if it was on Godot that wouldn't be the case."

THE TEST LOOP IS THE PRODUCT'S REAL DEPENDENCY ON THE WEB. Every ruling this project has is
settled on the device - rendering, audio mix, layout and feel are judged nowhere else - and the
whole of that loop is a push to `main` and a reload. A Godot build costs an export, a signing
step and an install for every one of those. So the web build is not merely the design document
the Godot version is made from: it is the instrument the design is judged with.

WHICH SETTLES THE ORDER, NOT THE DESTINATION. [[RLG-002]] keeps both exit routes and this does
not close either. What it does is remove any case for converting EARLY to solve a frame rate.
The fill-rate work above stays first, it is worth having on both sides, and it keeps the loop
the owner is relying on intact while it happens.

AND IT PROMOTES ONE PIECE OF ADVICE ABOVE FROM SENSIBLE TO REQUIRED. A per-pass cost
breakdown has to run in the browser, on the device, and print its numbers where the owner can
read them - because the slow machine is an iPad in someone else's hands and no harness here
will ever be run on it.
- **RULED 2026-09-01T17:56:09-04:00** — DEFERRED BY THE OWNER, 2026-09-01: "Either way it's fine for now. We'll keep building and do
that optimization pass we already wanted to do after more task cues have been completed."

So this does not jump the queue. It is a pass to be run once more of the recorded work is done,
and the queue in the thread stands as it is.

AND THE DEVICE MODEL IS NOT KNOWN AND WILL NOT BE. The owner says only that the iPad is
several years old. That is not a gap to be chased - it settles the METHOD instead. Nothing in
this pass may depend on naming the machine, which means two things: the desktop must be
throttled to stand in for a slow device, and the real numbers have to be readable on the iPad
itself, from inside the browser, because no harness here will ever run on it.

ONE MEASUREMENT WAS TAKEN BEFORE THE PASS WAS DEFERRED, AND IT SHOULD LEAD IT. The suspects
above are all about how MUCH work each pixel costs. This is about how many pixels there are.

`resize()` sets the canvas backing store to its CSS box times `devicePixelRatio`, capped at 2,
and nothing caps the box itself. `#stage` is `inset: 0` and `--stage-h` is `100dvh` less the
title bar, so the forward view fills whatever viewport it is given.

    On the owner's phone that is roughly 440 by 956 CSS at a capped ratio of 2, so about 1.7
    megapixels.

    A portrait iPad is around 820 by 1180 CSS at the same capped 2, so about 3.9 megapixels.

That is more than twice the pixels, before anything is drawn into them - and the road pass
paints the full screen about 150 times per frame, so the two multiply. A device that is
already slower is also being asked for over twice the work.

WHICH MAKES THE FIRST EXPERIMENT CHEAP AND OBVIOUS: cap the backing store and let the browser
scale the result up. A resolution cap is one number in `resize()`, it costs nothing anywhere
else, and it would separate "this iPad is slow" from "this iPad is being asked for twice as
much". If the frame rate comes back, the answer was never the drawing at all.

AND IT IS THE SAME QUESTION IN GODOT. A render resolution below the screen resolution is the
first thing any engine offers on a weak device, so this finding survives the conversion exactly
as the overdraw one does.
