+++
id                = "EVD-129"
kind              = "evidence"
status            = "recorded"
step              = "the garage card holds one height for every car, and the per-car height fails the check"
unit              = "UNT-079"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/title-test.py"
exit_code         = 0
duration_ms       = 10898
output_bytes      = 1240
output_sha        = "f496c88c1b46ccb9109b536c7de636cb"
store_stamp       = "e4b65672a9e31166"
created           = "2026-08-30T20:12:43-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-129 — step `the garage card holds one height for every car, and the per-car height fails the check` of [[UNT-079]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/title-test.py`
- Result: **exit 0**, in 10898 ms
- Output: 1240 bytes, sha256 `f496c88c1b46ccb9109b536c7de636cb`
- Store stamp at the time: `e4b65672a9e31166`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
title-test  .  the title card wears the engine own lamps
  browser: playwright bundled chromium (pinned)
      the player sprite declares: ['tail', 'turn.l', 'turn.r']
  ok    the player sprite declares a tail lamp
      the title car was drawn at {'x': 243.4, 'y': 450.6, 'w': 91.2, 'h': 66.4}
  ok    the title reports where it drew the car
      red pixels with the declaration in place: [548, 564, 557, 563]
  ok    the title car has lit lamps on it
      red pixels with the declaration stripped: [0, 0, 0, 0]
  ok    and they come from the sprite declaration, not from the title
  ok    the garage offers colours to tap
  ok    the engine hands over its sprites to be marked
      after tapping a colour: the lorry is the same object, the player rebuilt
  ok    a colour tap does not rebuild the traffic
  ok    and it does rebuild the car whose colour changed
      the tallest card is LORRY at 150 px, the shortest ROADSTER at 78, reserved 150
  ok    the reservation covers the tallest car in the fleet
  ok    and it is exactly that car, with nothing spare on top
      the drawn card across four cars: [150, 150, 150, 150]
  ok    the drawn card does not change height when the car does
  ok    no page errors

all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
