+++
id                = "UNT-070"
kind              = "unit"
status            = "closed"
cost              = 15714
closed            = "2026-08-30T16:35:57-04:00"
created           = "2026-08-30T16:23:47-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
delegate          = false
effort            = "high"
model             = "claude-opus-5"
ran_on            = "claude-opus-5"
routing           = "change"
+++

### UNT-070 - A bigger mirror, centred, and the HUD measures itself against it

- Routing class: `change` -> **claude-opus-5**, effort **high**. Bias 1. A policy or vocabulary change reaches every file that follows it. ([[RLG-092]])
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-30T16:35:57-04:00** - The mirror is larger and centred. It was never centred: viewShift moves the whole forward view left for the thumb cluster and the mirror's x was computed from it, so the glass hung 8.5 per cent left of the middle of a symmetrical windscreen - and a mirror is mounted on the screen rather than in the world. 0.62 capped at 250 becomes 0.80 capped at 340, with the height derived from the width at the shape the glass already had. The HUD measures itself against the glass now, because the row under it cleared a hardcoded 58 that was three numbers collapsed into one in a different file. For that gap to be assertable the HUD's top padding had to become the safe area alone, so the row owns the whole distance from the top - the gap is then exactly the 8 times scale it is designed to be, and the old hardcode reads minus eight. And the harness stopped sleeping through a transition it can watch instead.
