+++
id                = "RLG-143"
kind              = "ruling"
type              = "feature"
status            = "requested"
created           = "2026-09-01T13:54:01-04:00"
created_precision = "second"
source            = "owner, 2026-09-01"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = ["RLG-112", "RLG-105"]
+++

### RLG-143 — A bridge rises over water at ground level; a tunnel drops under ground level. One shape, negated
- **RULED 2026-09-01T13:54:40-04:00** — RULED 2026-09-01 - Owner, having driven the bridge:

"The bridge and the tunnel both have the same design in that the bridge starts as a plane of
water level to the ground and tarmac and the roadway of the bridge starts by going up high
above the water and then levels out for most of the duration of its existence where it's
verticality is capped to 0. Then at the end of the bridge, the roadway goes down to meet with
the next biome where the plane of water is still level with its ground and tarmac.

The tunnel does the opposite when you enter it goes down and then its verticality is capped to
0. And at the end of the tunnel, it moves back up to meet the ground of the next biome."

THIS IS ONE SHAPE WITH A SIGN, AND IT SETTLES A QUESTION THAT WAS LEFT OPEN. [[RLG-112]]
built the authored profile for the bridge and deliberately did NOT give the tunnel one,
because the owner had already capped a bore's form at 0.10 on both axes and one field would
have overruled that ruling. The owner has now stated the tunnel's shape directly and it is the
bridge's, negated: down, flat, up. So the tunnel takes a profile after all, and the earlier
caution was right to wait for this rather than to guess it.

AND THE WATER IS AT GROUND LEVEL, WHICH IS THE PART THAT CHANGES WHAT IS BUILT. v0.11.11 draws
the sea as a plane a stated thirteen camera heights below the deck - a constant, `WATER_DROP`,
chosen by eye. The owner's model has no such constant in it: the water is level with the
ground and tarmac of the place either side, and the DECK is what moves. The height of the
bridge above the sea is therefore not a number anybody picks. It is the integral of the
profile's own rise, and it is correct by construction at every point of the crossing including
the two ends, where it goes to zero because the road has come back down to meet the land.

THAT IS ALSO THE ANSWER TO A CONSTANT THAT WAS ALREADY SUSPECT. This fragment's own record
now names the same fault twice - a value typed in beside a value that is derived, which
drifts - and `WATER_DROP` is exactly that shape. Deriving the drop from the profile removes
it.

WHAT IT NEEDS FROM THE ENGINE, AND IT IS ONE ADDITION. The vertical is integrated in screen
space with no absolute altitude, and the authored profile REPLACES the slope inside an event -
so the road's accumulated height already carries the rise and there is nothing to subtract it
with. A second accumulator, integrating the ordinary rolled grade THROUGH the event as though
no profile were in force, gives the level the land would have been at. The water is drawn at
that, the deck at the road's own, and the gap between them is the bridge's height. The tunnel
needs no second surface, but the same accumulator tells it how far below the surrounding
ground its floor lies, which is what a mouth has to agree with.

TWO THINGS TO WATCH.

  THE MOUTHS ARE WHERE THIS WILL FAIL. Both places must MEET the neighbouring ground exactly,
  and the profile already returns to zero slope at both ends - but zero SLOPE is not zero
  HEIGHT, and it is the height that has to match. The ramp has to bring the road back to the
  level it left, or a bridge hands over to a desert from thirty feet up.

  AND THE CAP IS ALREADY BUILT FOR THE BRIDGE AND NOT FOR THE TUNNEL. "Verticality capped to
  0" is what the flat deck already does. The tunnel's own `hill` and `bend` stay at 0.10 by
  the earlier ruling; the profile overrides the vertical inside the event and the 0.10 goes on
  governing what little the bore does elsewhere. Do not remove it while adding the profile -
  it is not the same number answering the same question.
- **RULED 2026-09-01T15:17:51-04:00** — HALF BUILT, 2026-09-01, v0.11.14. The two profiles are in and the water is NOT. What is
missing and why is below, and it is a real obstacle rather than a thing left undone.

THE TUNNEL DOES THE OPPOSITE, AND IT IS THE SAME SHAPE NEGATED. A bore drops on entry, runs
flat, and comes back up to meet the next place. It is the bridge's own profile with the sign
turned over and nothing new was built for it - which is the test of whether the event
mechanism was a mechanism. Measured across the two crossings: the bridge runs 0 to +42 and
back to 0, the tunnel 0 to -24 and back to 0. A bore drops less than a span rises because a
bridge has to clear shipping and a tunnel only has to get under the ground.

AND THE MOUTHS MEET, WHICH THIS FRAGMENT NAMED AS WHERE IT WOULD FAIL. Zero SLOPE at each end
is not zero HEIGHT, and it is the height that has to match the place either side. The
profile's two ramps are equal and opposite, so the integral over a crossing is exactly zero -
checked at 0.000000 before any of this was wired up, and asserted on both places now.

THE 0.10 CAP STAYS ON THE TUNNEL, as this fragment said it must. The profile governs the
vertical inside the event; the cap governs what little the bore does outside it. Two numbers
answering two questions.

THE SECOND ACCUMULATOR IS BUILT. It runs the ordinary rolled grade straight through an event
as though no profile were in force, which is the level the land would have been at, and the
difference between it and the road is the crossing's own height.

AND THE WATER IS NOT DRAWN AT IT, WHICH IS THE HALF THAT FAILED. The obstacle is a units one
and it is worth stating exactly, because the next attempt will hit it in the first ten
minutes. Both accumulators are read through `hillPx`-style functions that SUBTRACT THE
PLAYER'S OWN VALUE - that is what makes the road drawn relative to the camera, and it is
correct for the road. It is wrong for the water: at the car's own position the subtraction
makes the difference zero, so the sea rose to meet the deck exactly where it should have been
furthest below it. The capture showed a causeway again.

WHAT IT ACTUALLY NEEDS. The camera sits a fixed height above the ROAD, so the water's depth
below the camera is the accumulated rise AT THE PLAYER'S POSITION, and that has to enter the
projection as a world height - the way `BORE.h` does for the tunnel's ceiling - rather than as
a screen offset added afterwards. The accumulators are in screen units, so the rise has to be
converted, or a third accumulator has to carry it in world units. THIS IS THE SAME MISTAKE THE
TUNNEL'S CEILING MADE AND THE NOTE IN `borePoints` RECORDS IT: a roof placed as an offset from
the road's own projected line rather than as a plane at a height. It cost three builds there.

`WATER_DROP` STANDS UNTIL THAT IS DONE, and it is still the constant this fragment objects to.
The bridge looks right with it; it is simply not derived, so it cannot be right at the mouths
the way the profile is.

AND THE OWNER HAS ADDED A CASE FOR THE TUNNEL, 2026-09-01: "we could do the water at land
level for the tunnel as well so long as it's drawn behind the tunnel entrance to suggest
you're entering a tunnel that's going under a body of water and then coming up out the other
side." IT COSTS ALMOST NOTHING ONCE THE WATER IS DERIVED. The bore already drops below the
land line and `drawBore` already paints after the road pass, so water drawn at the land level
is covered by the bore in the middle and visible around the mouth - which is the picture the
owner describes. It should be built with the water, not before it.
