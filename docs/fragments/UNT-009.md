+++
id                = "UNT-009"
kind              = "unit"
status            = "closed"
cost              = 5018
closed            = "2026-08-28T18:01:48-04:00"
created           = "2026-08-28T18:00:38-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = ["RLG-051"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
routing           = "feature"
+++

### UNT-009 - A time-of-day setting in the pre-race menu (RLG-051)

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-051]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-28T18:01:48-04:00** - A TIME control in the garage - DUSK, MIDNIGHT, DAWN, MIDDAY - setting where the day cycle starts, with the cycle running on as before. The four are the four phase points the engine already named, so no new lighting was written. It supersedes a deliberate decision that the sky keeps its own time across runs, and the replacement comment says what overruled it rather than deleting the argument. Verified by effect: phase 0.2517 against a set 0.25, and with the assignment disabled the label check still passed while the phase check failed - which is why the effect check exists. API.phase() was added to the seam for it. Two process facts are in the record: the unit was opened after the first edit, so its WIP baseline is not a clean starting point, and the suite was run directly rather than through step.py because RLG-039 is still open.
