+++
id                = "RLG-109"
kind              = "ruling"
type              = "feature"
status            = "requested"
created           = "2026-08-31T10:53:15-04:00"
created_precision = "second"
source            = "owner question, 2026-08-31"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### RLG-109 — A place should hand over to a place it could plausibly be next to
- **RULED 2026-08-31T10:53:59-04:00** — Owner, 2026-08-31: "As a rule for biome switching, we should try to not have unrealistic transitions,
like desert into tundra and vice versa. Maybe the way to do this is to assign biomes a temperature and
step between them on an axis I guess - what are your thoughts on that?"

THE INSTINCT IS RIGHT AND THE AXIS IS ALMOST ALREADY THERE. Every place already declares `rain` and
`snow`, and those two numbers ARE a climate position - snow is what cold means in this engine, and
rain is moisture. Laid out, they separate the places the owner wants separated without anything new
being invented:

    place       rain   snow
    DESERT      0.04   0.00      hot and dry
    TUNDRA      0.10   0.62      cold and dry
    COASTAL     0.34   0.00      mild and wet
    MOUNTAIN    0.30   0.34      cold and wet
    FOREST      0.42   0.06      mild and wet
    SWAMP       0.62   0.00      hot and wet
    CITY        0.38   0.10      no climate of its own

Desert to tundra is the largest jump on the `snow` axis on the board - 0.00 to 0.62 - which is exactly
the pair the owner named. That is a good sign that the numbers already know the answer.

BUT ONE AXIS IS NOT ENOUGH, AND THE REASON MATTERS. A single temperature line forces a TOTAL ORDER,
and a total order makes real neighbours impossible. Coastal to tundra is a perfectly ordinary piece of
the world - Norway, Iceland, the north Atlantic - and any ordering that puts desert and swamp at one
end and tundra at the other has to pass through the mild places to get anywhere. Two axes give
coast-and-cold for free; one axis forbids it.

AND SOME PLACES HAVE NO CLIMATE AT ALL. A city exists in every climate on earth. So do the two places
just approved: a tunnel is a hole in the ground and a canyon is rock, and neither says anything about
temperature. Forcing them onto the axis is the mistake that would make this feel arbitrary - a city
that cannot follow a desert is wrong in a way a player would notice faster than the thing being fixed.
The model needs a way for a place to say "anywhere".

SO THE RECOMMENDATION, AND IT IS ONE CHANGE TO ONE FUNCTION. Do not add a per-pair table. Ten places
is forty-five pairs, every new place makes somebody edit ten entries, and a table is a thing that goes
stale silently. Instead:

  DERIVE THE DISTANCE from `rain` and `snow`, weighting `snow` more heavily because temperature is
  what the owner actually noticed. That number already exists per place and needs no maintenance.

  WEIGHT THE PICK BY IT rather than forbidding jumps outright. `openBiome` and the biome roll choose
  uniformly from `BIOME_KEYS` today; choosing with a weight that falls off with distance keeps the
  world varied while making desert-to-tundra rare rather than illegal. A hard ban is brittle - it
  needs a fallback when nothing qualifies, and that fallback is where the arbitrary-looking choices
  come from.

  LET A PLACE DECLARE ITSELF NEUTRAL, one optional field, defaulting to not-neutral so nothing that
  exists changes. City, tunnel and canyon set it and can follow anything. That is the same shape
  `cover` took for the open sky: a tendency stated by the place, with everything else getting the
  ordinary behaviour for free.

IT ALSO MAKES THE TRANSITION LOOK BETTER, WHICH IS A SECOND ARGUMENT FOR IT. The crossing takes the
whole length of the drawn road: the two grounds blend across the band, the two skylines hand over by
sinking and rising, and the weather tapers between them. All three of those are interpolations between
two palettes, and they read better the closer the palettes are. So near-neighbour transitions are not
only more plausible, they are less work for the renderer to make convincing.

WHAT TO MEASURE. Not "is this transition realistic" - that is a judgement. What a harness can say is
the DISTRIBUTION: over a few thousand rolls, how often each pair comes up, and that the far pairs are
rare rather than absent. `biome-test` already samples the biome roll, so the instrument exists. The
check should also assert that every place is still reachable from every other by SOME route, or the
world quietly splits into two halves that never meet.

NOT STARTED. The owner asked for thoughts rather than for the change.
- **RULED 2026-08-31T11:05:25-04:00** — OWNER, 2026-08-31: "let's re-derive each biome's stats based on its climate. For example, if city is
truly neutral then it should have a .5 rain and snow. If tundra is cold and dry, then it should have a
lower chance for snow and an even lower chance for rain but still have a small baseline snow coverage
on the ground maybe like .2. Let's re-derive the entire biome list, including the new ones."

ONE CORRECTION FIRST, AND IT IS THE WHOLE REASON THIS NEEDS WRITING DOWN. "City is neutral, so 0.5
rain and 0.5 snow" would not make a city neutral - it would make it the snowiest place on the board.
`rain` and `snow` are not a position on a scale; they are PROBABILITIES that this place's weather roll
lands on rain or on snow. A city at snow 0.5 snows half the time, everywhere, all year. And cover is
derived from their sum, so it would also be permanently overcast: (0.5 + 0.5) * 1.6 clamps to the
0.75 ceiling.

THREE DIFFERENT QUANTITIES WERE BEING SPOKEN OF AS ONE, AND SEPARATING THEM IS THE FIX:

  WHERE A PLACE SITS IN THE CLIMATE - what decides which places can be next to each other. This does
  not exist yet and is what the owner is reaching for.

  HOW OFTEN IT RAINS OR SNOWS THERE - `rain` and `snow`, which exist and are probabilities.

  WHAT IS PERMANENTLY ON THE GROUND - `snowFloor`, which exists and is TUNDRA's alone at 0.50. This is
  the "small baseline snow coverage" in the owner's own sentence, and it is already a separate field.

SO THE POSITION IS NEW AND THE REST IS DERIVED FROM IT. Two axes, because one forces a total order and
a total order forbids coastal-and-cold, which is an ordinary piece of the world:

    temp   0 coldest, 1 hottest
    damp   0 driest,  1 wettest

And then, with no hand-tuned weather numbers left anywhere:

    P         = damp * 0.75                          how often it precipitates at all
    snowShare = clamp((0.50 - temp) / 0.50, 0, 1)    how much of that falls as snow
    snow      = P * snowShare
    rain      = P * (1 - snowShare)
    snowFloor = clamp((0.25 - temp) / 0.25, 0, 1) * 0.60

THE DERIVED TABLE. `cover` is the open-sky multiplier from the coastal work and stays a stated field,
because how much sky a place shows is about its shape rather than its climate - a forest is closed in
by trees whatever the weather.

    place      temp  damp  |  rain  snow  snowFloor  cover  |  today
    DESERT     0.95  0.05  |  0.04  0.00    0.00      1.00  |  0.04 / 0.00
    SWAMP      0.80  0.85  |  0.64  0.00    0.00      1.00  |  0.62 / 0.00
    CANYON     0.70  0.15  |  0.11  0.00    0.00      1.00  |  new
    FARMLAND   0.60  0.45  |  0.34  0.00    0.00      0.45  |  new
    COASTAL    0.55  0.50  |  0.38  0.00    0.00      0.45  |  0.34 / 0.00
    FOREST     0.45  0.70  |  0.47  0.05    0.00      1.00  |  0.42 / 0.06
    CITY       0.45  0.50  |  0.34  0.04    0.00      1.00  |  0.38 / 0.10
    MOUNTAIN   0.15  0.60  |  0.14  0.31    0.24      1.00  |  0.30 / 0.34
    TUNDRA     0.05  0.20  |  0.02  0.14    0.48      1.00  |  0.10 / 0.62
    BRIDGE      -    0.60  |  0.34  0.00    0.00      0.45  |  new
    TUNNEL      -     -    |  0.00  0.00    0.00       -    |  new, no sky

SEVEN OF THE NINE LAND WITHIN A FEW HUNDREDTHS OF THE NUMBERS ALREADY IN THE GAME, and that is the
strongest argument that this is a derivation rather than a rewrite. The values were hand-tuned one at
a time over weeks, and a two-axis model reproduces almost all of them.

TUNDRA IS THE ONE THAT REALLY MOVES, AND IT MOVES THE WAY THE OWNER ASKED. Snow falls from 0.62 to
0.14 and rain from 0.10 to 0.02, because a cold DRY place precipitates rarely - it is the ground cover
that makes it read as tundra, not constant snowfall. That is the owner's sentence turned into numbers:
"a lower chance for snow and an even lower chance for rain, but still a baseline coverage on the
ground". The derived floor is 0.48; the owner offered 0.2 as a guess. Keep it derived and tune the
0.60 coefficient if the ground reads too white - a number that falls out of the model can be moved in
one place, and a number typed into one biome cannot.

MOUNTAIN ALSO DROPS ITS RAIN, from 0.30 to 0.14, for the same reason: a cold place's precipitation is
mostly snow, and today's table has it doing both at once.

NEUTRAL IS A FLAG, NOT A MIDPOINT. City, tunnel and bridge state that they have no climate of their
own and may follow anything. That is what the owner meant by neutral and it is a different field from
`temp`. A city still needs weather numbers, so it keeps a temperate position for those - it is only
its ADJACENCY that is unconstrained. Canyon is given real numbers rather than the flag: a canyon is
genuinely an arid landform, and desert and mountain are the neighbours it should have.

A TUNNEL HAS NO WEATHER AT ALL, which falls out for free rather than needing a rule. There is no sky
above it, so rain 0 and snow 0 are facts about a roof rather than a climate. What happens to weather
already falling when you enter one is a real question and is left to [[RLG-105]].

WHAT THIS DOES NOT SETTLE. `hill` and `bend` are untouched - they are terrain rather than climate, and
nothing in the owner's request bears on them. And `cover` stays stated rather than derived for the
same reason: a forest is closed in by trees at every temperature.

NOT BUILT. This is the table the owner asked to see. Nothing in `BIOMES` has been changed, and the
adjacency rule this table exists to serve is still unbuilt as well.
- **RULED 2026-08-31T11:13:11-04:00** — OWNER, 2026-08-31, adjusting the table above: "truly neutral biomes should have an adjustable temp
each time that instance of that biome is generated, and instead of having a separate rain and snow
chance we could just call it precipitation and it's based off of the temperature. So we have a
temperature and a precipitation chance - we derive the whole table with that in mind."

THIS IS BETTER THAN WHAT IT REPLACES, AND IT REMOVES A FIELD RATHER THAN ADDING ONE. `rain` and `snow`
stop being stored at all. A place states how OFTEN it precipitates and how WARM it is, and what falls
out of the sky follows from the two. Two numbers per place, down from two probabilities plus a damp
axis.

AND IT DELETES AN IMPOSSIBLE STATE THAT EXISTS TODAY. MOUNTAIN currently declares rain 0.30 AND snow
0.34 - two independent rolls that sum to 0.64, so the place is asked twice whether it has weather. One
precipitation chance asked once, then split by temperature, cannot express that and does not need to.

THE MODEL, COMPLETE:

    stated per place    temp        where its temperature sits, 0 coldest to 1 hottest
                        tempVary    how far a given instance may roll either side of it
                        precip      how often it precipitates at all
                        cover       the open-sky multiplier, which is about shape not climate

    rolled per instance temp        temp + rnd(-tempVary, +tempVary), once, when the place opens

    derived             snowShare = clamp((0.50 - temp) / 0.50, 0, 1)
                        snow      = precip * snowShare
                        rain      = precip * (1 - snowShare)
                        snowFloor = clamp((0.25 - temp) / 0.25, 0, 1) * 0.60
                        cloudBase = precip * 1.6 * cover

`cover` NOW DERIVES FROM ONE NUMBER INSTEAD OF TWO. It was `(rain + snow) * 1.6`, and `rain + snow` is
exactly `precip`. So the cloud calculation gets simpler as a side effect rather than needing a change.

THE TABLE, RE-DERIVED:

    place      temp  vary  precip |  rain  snow  floor  cover
    DESERT     0.95  0.05   0.04  |  0.04  0.00   0.00   1.00
    SWAMP      0.80  0.08   0.64  |  0.64  0.00   0.00   1.00
    CANYON     0.70  0.20   0.11  |  0.11  0.00   0.00   1.00
    FARMLAND   0.60  0.20   0.34  |  0.34  0.00   0.00   0.45
    COASTAL    0.55  0.20   0.38  |  0.38  0.00   0.00   0.45
    FOREST     0.45  0.15   0.52  |  0.47  0.05   0.00   1.00
    CITY       0.45  0.45   0.38  |  varies with the roll   1.00
    MOUNTAIN   0.15  0.10   0.45  |  0.14  0.32   0.24   1.00
    TUNDRA     0.05  0.05   0.15  |  0.02  0.14   0.48   1.00
    BRIDGE     0.45  0.45   0.45  |  varies with the roll   0.45
    TUNNEL      -     -     0.00  |  0.00  0.00   0.00    -

WHAT A ROLLED TEMPERATURE ACTUALLY BUYS, shown on the city, which is the case the owner named:

    this city rolls   temp 0.05  |  rain 0.03  snow 0.35  floor 0.48    a city in the cold
    this city rolls   temp 0.45  |  rain 0.34  snow 0.04  floor 0.00    a temperate city
    this city rolls   temp 0.85  |  rain 0.38  snow 0.00  floor 0.00    a city in the heat

One entry in the table, three places to drive through, and nothing in the code names any of them.

AND THE `neutral` FLAG DISAPPEARS, WHICH IS THE BEST PART OF THIS. The previous entry proposed a
boolean saying "this place may follow anything". It is not needed: `tempVary` says the same thing
better and by degrees. A place with a narrow range - tundra, desert - can only sit near places of its
own temperature. A place with a wide one - city, tunnel, bridge - can sit anywhere, because it can
roll a temperature that fits wherever it lands. Neutrality stops being a special case and becomes a
number, which is the shape everything else in this table already has.

THE STRONGER VERSION, AND IT IS ONE LINE FURTHER. If a neutral place rolls its temperature TOWARD the
place it is following rather than uniformly within its range, then neutral places become the
connective tissue of the world: a city between a desert and a mountain rolls cool, and the crossing
either side of it is short in temperature terms. That is [[RLG-109]]'s adjacency problem partly solving
itself - the awkward transitions get a place to happen in.

WHERE THE ROLLED VALUE HAS TO LIVE. `BIOMES` becomes a table of RECIPES rather than of places: what a
city is like in general. The instance - this city, with its rolled temperature and the rain, snow and
floor that follow - has to be stored where the current place is stored, and computed once when the
place opens rather than per frame. `openBiome` already exists and already rolls the sea's side per
instance, so there is a precedent and a home for it.

AND THAT IS A REAL REFACTOR, NOT A TABLE EDIT. `B.rain` and `B.snow` are read by `rollWeather`,
`rollSky`, `weatherTaper` and `biome-test`, and every one of them currently reads the RECIPE. They all
have to read the INSTANCE instead, or a place will roll cold and rain anyway. That is the part of this
that can go quietly wrong, and it is the reason to build it as its own unit with the biome checks
watched failing first.

STILL NOT BUILT. Nothing in `BIOMES` has changed.
- **RULED 2026-08-31T11:26:56-04:00** — OWNER, 2026-08-31, on the table above: "I wonder if snow is too rare. Like for example, farmland can be
snowy, but you have snow chance at zero. I think we need wider variance to the temperatures for some
of these locations. I also don't understand what cover means." And separately: the biomes must still
drive their bendiness and verticality, which should be in the table for transparency.

THE SNOW OBSERVATION FINDS A REAL HOLE, AND IT IS NOT SOLVED BY WIDER VARIANCE. Above, `snowShare` is
computed from the PLACE's temperature, so anything warmer than 0.50 never snows at all - farmland at
0.60 is dry-locked out of snow no matter how the instance rolls. Widening `tempVary` would help a
little and would do it by making farmland sometimes be a COLD PLACE, which is not what happens in the
world. Temperate farmland is temperate all year and still gets snow in January.

SO THERE ARE TWO TEMPERATURES AND THE MODEL ONLY HAD ONE. A place has a climate; a day has weather.
Conflating them is why snow vanished.

    temp        the PLACE's climate, stated in the table
    tempVary    how much THIS INSTANCE of the place differs - a cold city or a hot one
    swing       how far a SINGLE WEATHER EVENT departs from that instance's temperature

The first two are the owner's per-instance roll and are unchanged. `swing` is new, it is GLOBAL rather
than per place - every place has weather, and one more per-place field earns less than it costs - and
it is rolled with each weather event rather than with the place.

    instance temp = temp + rnd(-tempVary, +tempVary)      once, when the place opens
    moment temp   = instance temp + rnd(-swing, +swing)   once, per weather event
    snowShare     = clamp((0.50 - moment temp) / 0.50, 0, 1)

With `swing` at 0.25, farmland at 0.60 has moments between 0.35 and 0.85, so roughly three
precipitation events in ten fall cold enough to snow, lightly. Which is what a temperate place does.
Desert at 0.95 has moments between 0.70 and 1.20 and never snows, which is also correct. Tundra at
0.05 is below the line at every moment. THE SAME NUMBER FIXES ALL THREE, which is the test of whether
a model is right or merely tuned.

AND IT MAKES SNOW AN EVENT RATHER THAN A PROPERTY, which is the better game. A snowy day in the
farmland is a thing that happens to you; a place where it always snows is scenery.

WHAT `cover` MEANS, since it was not clear and the name is the reason. It is CLOUD, not snow, and it
answers a question the rainfall cannot: how much of the sky is filled. An overcast day with no rain in
it is the commonest sky there is - the code says so where the cloud is rolled - so cloud cannot simply
be "is it raining". Today it is derived from how often a place precipitates, and this field is a
multiplier on that: the coast is at 0.45 because a coast is bright BETWEEN its showers, where a
rainforest that rains as often sits under cloud all day.

THE NAME IS BAD AND SHOULD CHANGE. `cover` reads as ground cover next to `snowFloor`, and a multiplier
where 0.45 means CLEARER is backwards to anybody reading it for the first time. `cloudBias` says what
it is: this place's tendency to be cloudy, relative to its own rainfall. 1.00 is ordinary, below 1 is
clearer than its rain suggests, above 1 is grey without needing to rain.

AND THE TWO TERRAIN AXES DO EXIST AND ARE UNCHANGED. `hill` and `bend` already scale a segment's
magnitude as it is generated, from the biome at the place that segment will be, and MOUNTAIN at 1.00
on both IS the road as it has always been - everywhere else is calmer. They belong in the table.

BETTER WORDS FOR THEM, since the owner asked for one. The geomorphology terms are exact and short:
RELIEF is the vertical variation of terrain, and SINUOSITY is how much a course winds. So the two axes
are relief and sinuosity, and the field names `hill` and `bend` can stay as they are - a name in a
table and a word in a conversation do not have to be the same, and renaming load-bearing fields for
vocabulary is not worth the churn.

THE WHOLE TABLE, WITH EVERYTHING STATED IN IT:

    place      temp  vary  precip  bias | relief  sinuosity | what it is
    DESERT     0.95  0.10   0.04   1.00 |  0.45     0.40    | hot, dry, open, rolling dunes
    JUNGLE     0.88  0.06   0.78   1.20 |  0.55     0.80    | hot, wettest, closed canopy
    SWAMP      0.80  0.10   0.64   1.10 |  0.15     0.70    | hot, wet, flat, half water
    CANYON     0.70  0.25   0.11   0.80 |  0.50     0.95    | arid, enclosed, the wall is the skyline
    FARMLAND   0.60  0.25   0.34   0.45 |  0.10     0.35    | temperate, open, the flattest place
    COASTAL    0.55  0.25   0.38   0.45 |  0.18     0.60    | mild, a sided sea, bright between showers
    FOREST     0.45  0.20   0.52   1.00 |  0.70     0.85    | cool, wet, the heaviest scenery
    CITY       0.45  0.45   0.38   1.00 |  0.30     0.30    | no climate of its own, rolls one
    MOUNTAIN   0.15  0.15   0.45   1.00 |  1.00     1.00    | cold, wet, the reference road
    TUNDRA     0.05  0.10   0.15   0.90 |  0.80     0.75    | coldest, DRY, ground white year round
    BRIDGE     0.45  0.45   0.45   0.45 | authored 0.10     | a fixed profile, water underneath
    TUNNEL      -     -     0.00    -   | authored 0.20     | no sky, no weather, dark by day

THE VARIANCES ARE WIDER THAN THE FIRST VERSION, which is the owner's other point and it is right. They
were set narrow out of caution and that made every place the same place every time. Desert and tundra
stay narrowest because they are defined by their extremes; farmland, coastal and canyon get 0.25 so a
run through one is not always the same run.

BRIDGE AND TUNNEL HAVE AUTHORED RELIEF, NOT A NUMBER, and that is the point made in [[RLG-112]]: a
ramp-flat-ramp span and a tunnel bore are shapes rather than tendencies. Their sinuosity stays low and
stated, because a bridge that wanders is a bridge nobody built.

STILL NOT BUILT.


A WORD THAT HAS BEEN USED THROUGHOUT AND WAS NEVER DEFINED. The owner asked what "roll" means, and it
is dice jargon that leaked out of the code into the conversation. `rollWeather`, `rollSky` and
`rollSeaSide` are real function names in `road.js`, so the word is established in the source - but it
was never explained to the person reading the reports.

TO ROLL A VALUE IS TO PICK A RANDOM NUMBER FROM A RANGE, ONCE, AT A PARTICULAR MOMENT, and then keep
it until something decides to pick again. Three things in this design roll, and WHEN each one rolls is
the whole design:

    the place's instance temperature   rolled ONCE, when you drive into that place
    the weather                        rolled every 35 to 80 seconds
    the moment's temperature           rolled with each weather event, deciding rain or snow

So "a city rolls its temperature" means: the moment you enter a city, the game picks one number
between 0.00 and 0.90, and that city is that temperature for as long as you are in it. Drive into the
next city and it picks again. Nothing is being rolled continuously and nothing is random per frame -
this project has a standing note that a per-frame random reads as a rendering fault rather than as
weather.

AND THE SIGN-OFF MAKES THIS ONE ITEM. Owner, 2026-08-31: once the table is agreed, the whole thing -
the two-temperature model, the derived weather, the adjacency rule and the new places - is a single
"biome and climate overhaul" item on the next queue rather than a dozen separate ones. That is the
right shape: the refactor underneath them is shared, and building the places one at a time on top of
the old table would mean doing the refactor last, when it is most expensive.
- **RULED 2026-08-31T11:34:18-04:00** — OWNER, 2026-08-31, on the cloud field: "cloudBias should be mostly driven by the weather itself I
suppose, unless it's strictly talking about non-weather cloud coverage - wispy white clouds. We should
probably always allow for some clear sky, no matter what. Obviously not including the tunnel."

THE FIRST HALF DIAGNOSES THE FIELD OUT OF EXISTENCE, AND IT IS RIGHT. Cloud already derives from how
often a place precipitates. A per-place multiplier on top of that was a second opinion about the same
thing, and the owner has correctly asked what it is FOR - which is a question it cannot answer, because
it was standing in for something else.

TWO DIFFERENT SKIES WERE SHARING ONE NUMBER, and that is the actual finding:

    STORM CLOUD is the overcast that comes with weather. Grey, thick, it hides the sun, and it is
    entirely a consequence of what the sky is about to do. It belongs to the weather and nothing else.

    FAIR-WEATHER CLOUD is the wispy white the owner names. It carries no rain, it does not darken
    anything, and it has nothing to do with whether it is going to rain. It belongs to the PLACE - a
    desert sky is empty, a jungle is full of vapour, a mountain wears a cap.

One `cloud` number was doing both jobs. That is precisely why the coast needed a bias to be "bright
between showers": the engine had no way to say CLEAR OF STORM CLOUD BUT NOT AN EMPTY SKY, so the only
lever was to make the whole thing thinner.

SO `cloudBias` IS DELETED AND `fairCloud` REPLACES IT, which is a smaller field doing an honest job:

    stormCloud = precip-driven, rolled with the weather, exactly as the owner asks
    fairCloud  = stated per place: how much white, rainless cloud its sky carries
    cloud      = clamp(max(stormCloud, fairCloud), 0, CLOUD_MAX)

AND THE COAST DOES NOT LOSE ITS BRIGHTNESS BY LOSING THE BIAS, which is worth checking rather than
assuming. The bias existed because coast and forest were 0.34 and 0.42 apart under the old table - a
whisker - so the coast needed a thumb on the scale to read differently. Under the new table they are
0.38 and 0.52, and the jungle is 0.78. The separation the bias was faking now falls out of the
precipitation numbers themselves, so removing it costs nothing.

THE SECOND HALF IS A CEILING, AND TODAY THERE IS NONE. `cloudTarget` is `base * rnd(0.35, 1.5)` and
the multiplier's LOW end is 0.35 - a proportion, not a floor - so a wet place can never roll a clear
sky. A jungle at base 0.75 has a minimum cover of 0.26 and would be under cloud for ever. The owner is
describing a real limitation rather than a preference.

TWO CHANGES GIVE IT, and they are at opposite ends:

    THE ROLL REACHES ZERO. `rnd(0, 1.25)` instead of `rnd(0.35, 1.5)`, so any place can roll a clear
    sky. A wet place still spends more of its time cloudy, because its base is higher - the
    FREQUENCY differs, and the possibility does not.

    THE CEILING IS BELOW TOTAL. `CLOUD_MAX` at 0.88, so even a storm leaves some sky. That is the
    owner's "always allow for some clear sky" stated as one number in one place.

AND ROLLING A CLEAR SKY WHILE IT RAINS IS ALREADY IMPOSSIBLE, which is why the roll can safely reach
zero. `stepSky` takes `Math.max(cloudTarget, need)` where `need` comes from the falling weather, under
a note saying rain and snow need a sky to fall out of. So the floor while raining is enforced by the
weather rather than by the roll, and lowering the roll's low end cannot produce rain from a blue sky.

THE TUNNEL IS EXEMPT AND IT IS NOT A SPECIAL CASE. It has no sky at all - not a clear one and not a
cloudy one - so it does not participate in this calculation any more than it participates in rain.
That falls out of having no sky rather than needing a rule, which is the same shape as its rain and
snow both being zero.

THE TABLE, WITH THE FIELD REPLACED:

    place      temp  vary  precip  fairCloud | relief  sinuosity
    DESERT     0.95  0.10   0.04     0.06    |  0.45     0.40
    JUNGLE     0.88  0.06   0.78     0.30    |  0.55     0.80
    SWAMP      0.80  0.10   0.64     0.20    |  0.15     0.70
    CANYON     0.70  0.25   0.11     0.08    |  0.50     0.95
    FARMLAND   0.60  0.25   0.34     0.18    |  0.10     0.35
    COASTAL    0.55  0.25   0.38     0.22    |  0.18     0.60
    FOREST     0.45  0.20   0.52     0.15    |  0.70     0.85
    CITY       0.45  0.45   0.38     0.12    |  0.30     0.30
    MOUNTAIN   0.15  0.15   0.45     0.25    |  1.00     1.00
    TUNDRA     0.05  0.10   0.15     0.10    |  0.80     0.75
    BRIDGE     0.45  0.45   0.45     0.22    | authored  0.10
    TUNNEL      -     -     0.00      -      | authored  0.20

WHAT EACH SKY NOW DOES, as a sanity read rather than a claim:

    the desert   clear almost always; its ceiling is 0.08 and its floor 0.06
    the jungle   from 0.30 to the 0.88 cap - never empty, often heavy
    the mountain 0.25 of cap cloud at its clearest, up to the cap in a storm
    the tundra   0.10 at its clearest; a cold dry place has a big empty sky

A NOTE ON WHAT THIS COSTS, because it changes code that shipped four hours ago. `cover` was added in
0.10.22 as the coast's open sky and is in the build the owner is testing. This replaces it. That is
the right call - it was one field standing in for two ideas, and the owner found that by asking what
it meant - but it should be said plainly rather than quietly rolled into a larger change.

STILL NOT BUILT.
- **RULED 2026-08-31T11:35:20-04:00** — OWNER, 2026-08-31, correcting the entry above: "Sure, but wispy white clouds that may or may not exist
are decoupled from the weather themselves."

THAT IS A CORRECTION AND THE ENTRY ABOVE HAS IT WRONG. It wrote
`cloud = clamp(max(stormCloud, fairCloud), 0, CLOUD_MAX)`, which does two things the owner has just
ruled out. Taking the MAX of the two couples them - the white cloud is only visible when the storm
cloud is thinner than it - and it makes `fairCloud` a permanent FLOOR, so a place with a value of 0.30
has at least that much cloud for ever. "May or may not exist" is the opposite of a floor.

SO THEY ARE TWO LAYERS AND NOT TWO TERMS IN ONE SUM. That is the whole of the correction, and it is
also why the first version reached for a max: it was still thinking of cloud as ONE quantity with two
contributors, when the owner is describing two things that happen to be in the same sky.

    STORM CLOUD   from the weather, and from nothing else. It DARKENS - it is what the sky-darkening
                  and the sun's occlusion already read. Its ceiling is CLOUD_MAX so there is always
                  some sky.

    WHITE CLOUD   its own layer, on its own timer, with its own roll. It DOES NOT DARKEN ANYTHING. It
                  is drawn, and it may not be there at all.

AND THE DECOUPLING NEEDS NO ARBITRATION BETWEEN THEM, which is the part that makes this simpler rather
than more complex. Nothing has to decide which layer wins. The white cloud is drawn, the storm cloud
is drawn over it, and an overcast sky hides the white cloud for the same reason an overcast sky hides
everything else - not because a formula suppressed it.

WHAT THE TABLE COLUMN MEANS NOW, because it changes: `fairCloud` is not how much white cloud a place
has. It is the MOST it tends to carry, and the roll runs from nothing up to it:

    whiteAmount = fairCloud * rnd(0, 1.4), rolled on its own timer, clamped at 1

So a desert at 0.06 is usually an empty blue sky with the occasional wisp; a jungle at 0.30 is usually
carrying something. Neither is guaranteed either way on any given roll, which is what "may or may not
exist" asks for.

THE SANITY READ IN THE ENTRY ABOVE IS WITHDRAWN. It said "the desert's floor is 0.06" and "the jungle
never has an empty sky". Both were consequences of the max, and both are wrong now: every place can
have a completely clear sky, and the difference between places is how OFTEN and how MUCH, never
whether.

AND THE CEILING BELONGS TO THE STORM LAYER ONLY. `CLOUD_MAX` at 0.88 keeps some sky visible during a
storm. The white layer needs no ceiling, because it does not darken anything - a sky full of white
cloud is still a bright sky, which is the distinction the owner drew in the first place.

THE TABLE IS UNCHANGED IN ITS NUMBERS. Only the meaning of the `fairCloud` column moves, from a floor
to a ceiling on its own roll.

STILL NOT BUILT.
- **RULED 2026-08-31T11:39:18-04:00** — SIGNED OFF BY THE OWNER, 2026-08-31: "I'm good with all that." The table and the model above are
agreed as they stand.

THE WHOLE OF IT IS ONE ITEM ON THE NEXT QUEUE, not a dozen. The BIOME AND CLIMATE OVERHAUL carries:

    the two-temperature model      a place has a climate, a day has weather
    the derived weather            rain, snow and the ground floor all fall out of temp and precip
    the two sky layers             storm cloud from the weather, white cloud from the place
    the clear-sky floor and ceiling any place can be clear, no place is ever total
    the adjacency rule             a place hands over to one it could plausibly be next to
    the five new places            [[RLG-102]] farmland, [[RLG-104]] canyon, [[RLG-105]] tunnel,
                                   [[RLG-112]] bridge, [[RLG-113]] jungle

AND THE ORDER INSIDE IT IS NOT FREE. The refactor comes first, because every place after it reads the
new fields; farmland next, because it is the only new place needing no new answer from the engine and
it proves the sided mechanism generalises; then the tunnel, because the jungle's canopy asks the same
question its bore does and building the jungle first would give the engine two answers to one
question; then canyon, bridge and jungle in any order, except that all three want the continuous
skyline form and whichever is built first should build it properly.

THE CURRENT QUEUE FINISHES FIRST. Owner, 2026-08-31: get back to the current queue, finish it, and
this starts after.
- **RULED 2026-08-31T20:01:09-04:00** — THE BUILD ORDER, WRITTEN BEFORE A CONTEXT CLEAR AND NOT STARTED. Owner, 2026-08-31: "I'm not sure if we have the runway to complete that major overhaul before the context window closes, so maybe we just document it and start it after it cleared." Correct, and the reason is worth stating: this refactor's failure mode is a HALF-MIGRATED `road.js`, where some readers take the recipe and some take the instance. That state is silent - a place rolls cold and rains anyway - and it is much worse than not having started.

NOTHING IN `road.js` HAS BEEN CHANGED. What follows is the site list, found by reading, so the next session does not have to find it again.

EVERY PLACE THAT READS `rain`, `snow`, `cover` OR `snowFloor` TODAY, in the order they should be changed:

  1. `BIOMES`, road.js ~8134. The recipes gain `temp`, `vary`, `precip` and `bias`; they lose `rain`,
     `snow`, `cover` and `snowFloor`. `hill` and `bend` stay exactly as they are. The full table is
     in the entry above - nine places plus BRIDGE and TUNNEL, which are for later units.

  2. `openBiome`, ~8652. Rolls the instance. `rollSeaSide()` is called here already and is the exact
     precedent: a thing decided once when the place opens, not per frame.

  3. `stepBiome`, ~8676. This is where `biomeTo` is chosen when a change is PLACED, and it is the
     second place an instance has to be rolled. Missing it is the likeliest way to get this wrong:
     the place you are driving INTO would have no instance and would fall back to the recipe.

  4. `snowFloorNow`, ~8284. Reads `.snowFloor` off both recipes and blends. Must read the two
     INSTANCES instead, because the floor is now derived from the instance's temperature.

  5. `endImpossibleWeather`, ~8510. `B.snow <= 0` and `B.rain <= 0`. These are CAPABILITY questions -
     can this place snow at all - so they need a helper rather than a chance: with `swing` applied,
     a place can snow if `precip > 0` and `instance temp - swing < 0.50`.

  6. `weatherTaper`, ~8767. `const support = snowy ? Bnew.snow : Bnew.rain`, compared against
     `WEATHER_KEEP` and `WEATHER_THIN`. Needs the instance-derived chance for the place being
     entered.

  7. `rollSky`, ~8857. `const base = clamp((B.rain + B.snow) * 1.6, 0.05, 0.75)` becomes
     `precip * 1.6` - the entry above notes this gets SIMPLER, because `rain + snow` was always
     exactly `precip`. And `const open = B.cover` becomes `B.bias`.

  8. `rollWeather`, ~8955. The heart of it: `r < B.snow` and `r < B.snow + B.rain` become the moment
     roll - instance temp plus `rnd(-swing, +swing)`, then `snowShare`, then the two chances. The
     `optWeather === 'wet'` line below it also reads `B.snow > B.rain` and needs the same treatment.

  9. The API, ~18150, ~18210 and ~18216. `API.snowFloor`, the taper probe's
     `support = isSnow ? B.snow : B.rain`, and `return { name:B.name, rain:B.rain, snow:B.snow }`.
     The last one is what `biome-test` reads, so it decides whether the harness is testing the
     recipe or the instance - and testing the recipe is exactly the bug this refactor can hide.

WATCH THE BIOME CHECKS FAIL FIRST. `tools/biome-test.py` and `tools/weather-test.py` both sample the
weather odds. Change the API in step 9 LAST and run them before and after each of the earlier steps,
because a check that keeps reading the recipe will stay green through a broken migration - which is
the precise shape of the failure this ruling warned about.

AND THE ORDER MATTERS FOR ONE MORE REASON. Steps 1 and 2 alone leave the game running on recipes that
no longer have `rain` or `snow`, so everything downstream reads `undefined` and the weather stops. It
is not shippable until step 9. This is a single unit and a single commit, and it should not be split.

TWO NUMBERS THAT ARE NEW AND GLOBAL, both tunables with committed defaults: `swing` at 0.25, how far
a single weather event departs from the instance's temperature, and the 0.60 coefficient on
`snowFloor`. The entry above explains why both are global rather than per place.
- **RULED 2026-08-31T20:36:55-04:00** — BUILT, 0.10.38, AND IT WENT IN AS ONE UNIT AND ONE COMMIT. The build order written before the
context clear was followed as written and needed no correction: the nine sites it listed were the
nine sites that existed, in the order it gave them, and changing the API last was the right call for
the reason it stated.

WHAT IS IN THE TABLE NOW, seven places rather than the twelve the model describes. Farmland, canyon,
tunnel, bridge and jungle are later units and their rows are not written yet:

    place      temp  vary  precip  bias  ->  rain   snow   floor
    DESERT     0.95  0.10   0.04   1.00      0.040  0.000  0.00
    SWAMP      0.80  0.10   0.64   1.10      0.640  0.000  0.00
    COASTAL    0.55  0.25   0.38   0.45      0.380  0.000  0.00
    FOREST     0.45  0.20   0.52   1.00      0.468  0.052  0.00
    CITY       0.45  0.45   0.38   1.00      0.342  0.038  0.00
    MOUNTAIN   0.15  0.15   0.45   1.00      0.135  0.315  0.24
    TUNDRA     0.05  0.10   0.15   0.90      0.015  0.135  0.48

Every derived figure reproduces the agreed table exactly, which is the first evidence that the model
was arithmetic rather than aspiration.

THE FIELD IS `bias` AND NOT `fairCloud`, AND THAT IS A DELIBERATE NARROWING RATHER THAN AN OVERSIGHT.
The signed-off model deletes the per-place cloud multiplier and replaces it with a SECOND DRAWN
LAYER: storm cloud from the weather, white cloud from the place, decoupled, the white one on its own
timer and darkening nothing. That is a rendering feature and it is not built. Deleting the multiplier
without building the layer would have taken the coast's open sky away - shipped four hours earlier in
0.10.22, and in the build the owner is testing - and given nothing back until a later unit. So the
field was RENAMED and kept, the build order's own step 7 says `bias`, and the owner's stated next
action names `temp`, `vary`, `precip` and `bias`. THE WHITE CLOUD LAYER REMAINS UNBUILT and is the
next thing this fragment owes.

TWO NUMBERS FROM THE LATER RULING WERE TAKEN, because the refactor could not leave them. The roll
now reaches zero and `CLOUD_MAX` is 0.88. They are two lines inside the two functions this unit was
already rewriting, they are signed off, and without them the coast fails its own brightness check:
the coast's precipitation rose from 0.34 to 0.38 under the new table, and against the old roll's
low end of 0.35 that pushed it from clear on 58% of rolls to 49%. The owner's "always allow for some
clear sky" and the coast's open sky turn out to be the same fix.

THE THRESHOLDS HAD TO MOVE AND THAT WAS NOT FORESEEN. `WEATHER_KEEP` and `WEATHER_THIN` were 0.30
and 0.05 against odds typed into the table; the derived odds are on a different scale, because one
precipitation chance is now SPLIT between rain and snow where the old table stated two chances and
let them sum past what the place should have had. Tundra snow was 0.62 and is 0.135. Left alone, the
thresholds would have silently reversed the owner's three cases and started tapering snow as it
entered a tundra. They are 0.12 and 0.02 now, and the BEHAVIOUR is what the checks assert, which is
the right way round.

ONE CASE CHANGES AND CANNOT BE AVOIDED. Rain into a DESERT now thins over the whole crossing instead
of a third of it. Desert rain is 0.04 and city snow is 0.038, so no threshold can separate them, and
of the two it is the city the owner ruled on by name. A desert does rain 4% of the time, so the
ordinary taper is the defensible half of the pair.

A TENDENCY IS NOT A CAPABILITY, AND THE OLD CODE HAD ONLY ONE OF THEM. `endImpossibleWeather` asked
`B.snow <= 0`, which was a fair question when the chance was stated and is the wrong question now: a
coast's instance snows 0.000 on average and a single cold moment within `swing` of that average snows
perfectly well. `canSnow` and `canRain` are stated once, in `climateAt`, rather than inferred from a
probability at each site. The visible consequence is that snow carried onto a mild coast is no longer
killed outright - it tapers quickly instead, which is what a mild coast should do to it.

THE FALSIFICATION, RUN RATHER THAN ASSERTED, AND IT IS THE FINDING OF THIS UNIT. `rollWeather` was
put back on the recipe - the exact half-migration this ruling was written to prevent - and
biome-test was run against it. ONE CHECK FAILED AND THE OTHER EIGHTY PASSED. The cold city and the
warm city both returned the recipe's answer, about a tenth snow, and nothing else in the suite
noticed. That is the silent failure stated as a measurement instead of as a worry, and it says the
guard is a single check rather than a property of the harness: `sampleWeatherRolls` calls the
engine's own `rollWeather` against whatever instance is running. Every other new check re-runs the
model through a probe, and a probe agrees with itself on a broken build.

biome-test is 81 of 81, from 64 before. Three of its old checks encoded the old table and were
rewritten rather than deleted: the snow floor is now an ordering rather than a list of one place,
the "can this place produce it" test asks the capability, and the coast's open sky is measured
against the CITY - which precipitates on exactly the same 0.38 of rolls, so the bias is the only
thing left that can separate them. The old form took the minimum of three places whose rainfall
differed, and measured the rainfall as much as the bias.

WHAT THE HARNESSES CANNOT JUDGE, and the owner should be asked in this order. Whether a tundra still
READS as a tundra now that it precipitates on 15% of rolls rather than 72%: the ground is white
either way, because the floor holds it, but the falling snow is now rare. Whether the mountain's new
0.24 of permanent ground snow is right or reads as a mountain that never melts. Whether the sky at
`CLOUD_MAX` 0.88 still reads as overcast rather than as a storm that never quite arrives. And
whether a temperate place snowing on a fifth of its falls is the rate the owner meant - `swing` at
0.25 is the one number that moves all of it.

weather-test WAS MEASURED ON BOTH ARMS, because the thread forbids quoting one run of it. Seven runs
before the change and four after: the baseline fails between one and five checks and this build fails
between one and three, and the failing check names overlap - the road against the land, the band
under the horizon, the far road against the near, the sky above the horizon. The baseline's worst run
was worse than anything this build produced. That is not a clean bill of health; it says the harness
still cannot tell a regression from noise here, and it says this change is not visible above that
noise.
