+++
id                = "EVD-131"
kind              = "evidence"
status            = "recorded"
step              = "the ordinary cars share one card and the big ones share a taller one, each exactly its own tallest"
unit              = "UNT-080"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/title-test.py"
exit_code         = 0
duration_ms       = 10857
output_bytes      = 1475
output_sha        = "35d27b495580fc7fc6dd7dadbbbce15c"
store_stamp       = "1ceb5790e44b4806"
created           = "2026-08-30T20:17:27-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-131 — step `the ordinary cars share one card and the big ones share a taller one, each exactly its own tallest` of [[UNT-080]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/title-test.py`
- Result: **exit 0**, in 10857 ms
- Output: 1475 bytes, sha256 `35d27b495580fc7fc6dd7dadbbbce15c`
- Store stamp at the time: `1ceb5790e44b4806`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
title-test  .  the title card wears the engine own lamps
  browser: playwright bundled chromium (pinned)
      the player sprite declares: ['tail', 'turn.l', 'turn.r']
  ok    the player sprite declares a tail lamp
      the title car was drawn at {'x': 243.6, 'y': 450.6, 'w': 91.2, 'h': 66.4}
  ok    the title reports where it drew the car
      red pixels with the declaration in place: [543, 552, 557, 566]
  ok    the title car has lit lamps on it
      red pixels with the declaration stripped: [0, 0, 0, 0]
  ok    and they come from the sprite declaration, not from the title
  ok    the garage offers colours to tap
  ok    the engine hands over its sprites to be marked
      after tapping a colour: the lorry is the same object, the player rebuilt
  ok    a colour tap does not rebuild the traffic
  ok    and it does rebuild the car whose colour changed
  ok    the fleet has both ordinary cars and oversized ones
      ordinary cars want up to 112 px (PICKUP), the big ones up to 150 px (LORRY)
      reserved: 112 for the ordinary cars, 150 for the big ones
  ok    the ordinary cars reserve exactly their own tallest
  ok    and the big ones reserve exactly theirs
  ok    and the two tiers are actually different sizes
      the drawn card across four ordinary cars: [112, 112, 112, 112]
  ok    the drawn card does not change height between ordinary cars
  ok    and it is the ordinary reservation, not the big one
  ok    no page errors

all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
