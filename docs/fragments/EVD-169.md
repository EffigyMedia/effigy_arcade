+++
id                = "EVD-169"
kind              = "evidence"
status            = "recorded"
step              = "the skyline checks still pass, and the frozen-clock control is steady now the clock can be held"
unit              = "UNT-093"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/skyline-test.py"
exit_code         = 0
duration_ms       = 13429
output_bytes      = 1304
output_sha        = "828dc43e152a43a302da4e38872baaae"
store_stamp       = "7d853c640952bd97"
created           = "2026-08-31T08:01:13-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-169 — step `the skyline checks still pass, and the frozen-clock control is steady now the clock can be held` of [[UNT-093]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/skyline-test.py`
- Result: **exit 0**, in 13429 ms
- Output: 1304 bytes, sha256 `828dc43e152a43a302da4e38872baaae`
- Store stamp at the time: `7d853c640952bd97`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
skyline-test  .  the city is the place's, not the hour's
  browser: playwright bundled chromium (pinned)
  the place: CITY
  buckets walked: 4 6 7 9 12 17 22 27 32 37

  ok    the run crossed several hour buckets, so the rebuild actually fired
  ok    the silhouette does not change as the day turns
        columns that moved across the whole day: 0 of 1024
  ok    and it still takes the hour's light, so the sprite is not simply frozen
        colour travelled 100 across the day  (rgb(46,29,46) -> rgb(58,37,51))
  ok    and the silhouette covers the same area at every hour

  the windows
  ok    the hour is dark enough for the city to be lit at all
  ok    windows switch on and off as the night goes on
        columns whose lit count changed: 73 of 1024
  ok    and the buildings do not move while their windows do
  ok    and they do not switch in unison
        lit pixels across the night: [300, 264, 306, 312, 276, 240]

  ok    with the window clock held still nothing switches, so the check reads time
        with the clock held at 120s: 0 columns changed

  the same check, with the plan forgotten between samples
  ok    with the plan re-rolled the silhouette check goes red, so it was measuring shape
        columns that moved: 1024 of 1024

  ok    no page errors

all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
