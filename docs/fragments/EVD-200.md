+++
id                = "EVD-200"
kind              = "evidence"
status            = "recorded"
step              = "a crossed checkpoint says what it paid you, with a number and a unit rather than a bare number"
unit              = "UNT-109"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/race-test.py"
exit_code         = 0
duration_ms       = 51023
output_bytes      = 826
output_sha        = "98dea4db1a9cb9dd5bfea342447229e6"
store_stamp       = "31bcbb14a4342cd2"
created           = "2026-08-31T17:24:53-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-200 — step `a crossed checkpoint says what it paid you, with a number and a unit rather than a bare number` of [[UNT-109]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/race-test.py`
- Result: **exit 0**, in 51023 ms
- Output: 826 bytes, sha256 `98dea4db1a9cb9dd5bfea342447229e6`
- Store stamp at the time: `31bcbb14a4342cd2`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
race-test  .  a race has checkpoints, whatever the toggle says
  browser: playwright bundled chromium (pinned)
  ok    the garage starts on TEST DRIVE
  ok    and TIMED can be turned off there
  ok    and back on again
  ok    then the mode changes to a race
      TIMED off then on, then a race: 1 board(s)
  ok    a race puts checkpoint boards on the road
  ok    TIMED can be left off
  ok    with the mode set to a race
      TIMED left OFF, then a race: 1 board(s), clock at 42.6
  ok    a race with TIMED off still puts boards on the road
      the checkpoint flash reads: 'CHECKPOINT  +20 SEC'
  ok    a checkpoint is actually crossed
  ok    and it names a unit rather than a bare number
      it awarded 20 SEC
  ok    and the unit is a word and the award is a real number
  ok    no page errors

  all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
