+++
id                = "EVD-192"
kind              = "evidence"
status            = "recorded"
step              = "the grid is at rest through the count and pulls away from it, the box is in neutral on the automatic gearbox with the engine free, and each of the three launch outcomes matches where the needle actually was"
unit              = "UNT-103"
command           = ".venv/Scripts/python tools/launch-test.py"
exit_code         = 127
duration_ms       = 7
output_bytes      = 69
output_sha        = "32d13edb610e49a5ef8a9e774e413581"
store_stamp       = "29441f287804c1a9"
created           = "2026-08-31T14:46:50-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-192 — step `the grid is at rest through the count and pulls away from it, the box is in neutral on the automatic gearbox with the engine free, and each of the three launch outcomes matches where the needle actually was` of [[UNT-103]]

- Ran: `.venv/Scripts/python tools/launch-test.py`
- Result: **EXIT 127 — the step FAILED**, in 7 ms
- Output: 69 bytes, sha256 `32d13edb610e49a5ef8a9e774e413581`
- Store stamp at the time: `29441f287804c1a9`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
could not run: [WinError 2] The system cannot find the file specified
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
