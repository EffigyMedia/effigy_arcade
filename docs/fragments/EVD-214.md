+++
id                = "EVD-214"
kind              = "evidence"
status            = "recorded"
step              = "both mechanisms land in one measured band in both cabinets, and the band follows the mirror"
unit              = "UNT-121"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/message-band-test.py"
exit_code         = 0
duration_ms       = 14162
output_bytes      = 1376
output_sha        = "98e20aaa17e3c8de534684f639bb8700"
store_stamp       = "9ecd11ced7604e92"
created           = "2026-08-31T21:44:08-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-214 — step `both mechanisms land in one measured band in both cabinets, and the band follows the mirror` of [[UNT-121]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe tools/message-band-test.py`
- Result: **exit 0**, in 14162 ms
- Output: 1376 bytes, sha256 `98e20aaa17e3c8de534684f639bb8700`
- Store stamp at the time: `9ecd11ced7604e92`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
message-band-test  .  a message sits under the mirror, not over the road
  browser: playwright bundled chromium (pinned)

  INTERSTATE
      mirror ends 66, information row 0-126, band 136px, banner top 136
  ok    the engine publishes a message band
  ok    the banner is BELOW the information row, which is where the owner put it
  ok    and it is still in the top half rather than pushed out of sight
  ok    and the canvas labels land where the DOM banner actually is
  ok    and it clears the horizon, so it is not over the road ahead
      at 360x780: mirror ends 57, band 116px
  ok    the band MOVES with the mirror, so it is anchored rather than placed
  ok    and it is still below the row at the new size
  ok    no page errors

  MOTORSPORT
      mirror ends 66, information row 0-116, band 126px, banner top 126
  ok    the engine publishes a message band
  ok    the banner is BELOW the information row, which is where the owner put it
  ok    and it is still in the top half rather than pushed out of sight
  ok    and the canvas labels land where the DOM banner actually is
  ok    and it clears the horizon, so it is not over the road ahead
      at 360x780: mirror ends 57, band 116px
  ok    the band MOVES with the mirror, so it is anchored rather than placed
  ok    and it is still below the row at the new size
  ok    no page errors

all checks passed
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
