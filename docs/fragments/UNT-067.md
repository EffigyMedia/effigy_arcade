+++
id                = "UNT-067"
kind              = "unit"
status            = "closed"
cost              = 19816
closed            = "2026-08-30T11:52:15-04:00"
created           = "2026-08-30T11:45:41-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
delegate          = false
effort            = "high"
model             = "claude-opus-5"
ran_on            = "claude-opus-5"
routing           = "change"
+++

### UNT-067 - The HUD clears what it is anchored to, at every scale

- Routing class: `change` -> **claude-opus-5**, effort **high**. Bias 1. A policy or vocabulary change reaches every file that follows it. ([[RLG-092]])
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-30T11:52:15-04:00** - The owner named the driving HUD and the capture named the elements: at 820 wide the controls are half as big again through --ark-ui while TIME and DISTANCE are the same 26px they are on a phone, in corners that have moved two hundred pixels out. The shell's own note says a game opts in by scaling its overlay, and this cabinet opted its controls in and left its readouts behind - half a job rather than a wrong number. Nothing was resized: every figure is what it was times the scale everything beside it already used, so a phone is unchanged to the pixel. The mirror clearance is deliberately NOT scaled, because the glass is drawn on the canvas at a fixed 44 and does not grow - an anchored distance follows what it is anchored to. Measured 26.0px at scale 1 and 46.8px at 1.8, against 26.0 and 26.0 with the fix removed. And the pedal pads run off the bottom edge at 820x1180, which is recorded rather than fixed: capping the cluster's scale changes how the controls feel on every large screen.
