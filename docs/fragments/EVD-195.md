+++
id                = "EVD-195"
kind              = "evidence"
status            = "recorded"
step              = "the engine sounds through the count-in, its pitch tracking the needle on the context the game is playing through, and the launch window still behaves"
unit              = "UNT-105"
command           = "P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/launch-test.py"
exit_code         = 0
duration_ms       = 41640
output_bytes      = 2248
output_sha        = "e9f03fa6072e49281378fd15ab60a8cf"
store_stamp       = "a52498ef61db86a0"
created           = "2026-08-31T15:08:06-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
+++

### EVD-195 — step `the engine sounds through the count-in, its pitch tracking the needle on the context the game is playing through, and the launch window still behaves` of [[UNT-105]]

- Ran: `P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/.venv/Scripts/python.exe P:/Code_Continuum/Projects/In-Dev/Effigy_Arcade/tools/launch-test.py`
- Result: **exit 0**, in 41640 ms
- Output: 2248 bytes, sha256 `e9f03fa6072e49281378fd15ab60a8cf`
- Store stamp at the time: `a52498ef61db86a0`

**Output** — head only; the digest above covers the whole of it, so this excerpt cannot be selective without saying so.

```
launch-test  .  the grid starts from rest and the player launches from neutral
  browser: playwright bundled chromium (pinned)

  the box, and the engine in it
  ok    the count runs long enough to launch in
  ok    the box is in neutral for the whole count, on the automatic gearbox
  ok    and the launch is armed while it is held
      needle with the pedal flat: 0.984 of the redline at its highest
  ok    a held throttle winds the engine to the limiter
      after lifting: 0.798 at its highest, 0.747 by the end of the count
  ok    and the needle falls again when the throttle is released
  ok    the engine voice exists while the count is up
      engine pitch across the count: 79.6 Hz to 435.9 Hz
  ok    and its pitch climbs with the revs rather than sitting at idle
  ok    on the context the game is playing through, not an orphaned one
  ok    and it is audible rather than held at zero
  ok    and the highest pitch belongs to the highest revs
  ok    and it idles when the throttle is never touched

  the window, and the three things that can happen at it
      none   needle 0.084   band 0.612-0.859   BOGGED
      flat   needle 0.957   band 0.612-0.822   WHEELSPIN
      aim    needle 0.736   band 0.612-0.800   PERFECT LAUNCH
  ok    the none start landed under the band and the game said so
  ok    the flat start landed over the band and the game said so
  ok    the aim start landed inside the band and the game said so
      speed a second after GO:  none 0   flat 331   aim 393
  ok    at least one of the three starts landed inside the window
  ok    a launch inside the window beats the none start
  ok    a launch inside the window beats the flat start

  the grid
  ok    a race grid is eleven rivals
  ok    every rival is at a standstill while the count is up
      at GO the fastest rival is at 0.017 of its target speed
  ok    and none of them is already at racing speed when it lets go
      a second later 11 of 11 are moving, the fastest at 0.128 of target
  ok    they all pull away once it does
  ok    and they are still building speed rather than snapping to it
      launch quality against grid position over 33 rivals: r = -0.274
... 3 more line(s), covered by the digest
```

*Written by `step.py` as a byproduct of running the step. **Nobody attested to this** ([[RLG-081]]).*
