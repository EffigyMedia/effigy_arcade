+++
id                = "UNT-084"
kind              = "unit"
status            = "closed"
cost              = 19878
closed            = "2026-08-30T20:59:17-04:00"
created           = "2026-08-30T20:50:23-04:00"
created_precision = "second"
source            = "owner report, 2026-08-30"
supersedes        = []
blocks            = []
caused_by         = ["RLG-090"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-084 - A run owns its weather, and a retry starts it clean

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-090]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-30T20:59:16-04:00** - A run owns its weather. Rain, snow, what has settled, the cloud, the storm and the place live in one freshWorld list the reset calls, and API.worldState reports exactly those fields so the check cannot drift from them. Three of these have been found one after another and a fourth line in reset() would have fixed one and left the next. retry-test dirties the world on purpose first - a dry run would have proved nothing - and with the weather inherited it fails on every field: wet 0.8, snowy 1, settle 0.9, cloud 0.95, storm 1. ALSO: the horizon snapped to the next biome at GO because biomeNext was left at zero, which means a change is due; it is armed when the opening place is chosen, and the check now watches the horizon as well as the ground because reading the place at the CAR passed a build whose far end was changing. AND THE BLACK SCREEN THE OWNER HIT WAS A HALF-UPDATED CACHE - the exact symptom sw.js predicts. Eight versions shipped this evening with the cache names untouched, because the rule asks whether a file MOVED and the real question is whether the files still AGREE. Caches are v27 and pack.sh refused the build until assets.js agreed, which would have caught it on any of the seven builds before, had it been run - it was not run once this session.
