+++
id                = "UNT-106"
kind              = "unit"
status            = "closed"
cost              = 45474
closed            = "2026-08-31T15:36:03-04:00"
created           = "2026-08-31T15:21:45-04:00"
created_precision = "second"
supersedes        = []
blocks            = []
caused_by         = []
see_also          = []
delegate          = false
effort            = "high"
model             = "claude-opus-5"
ran_on            = "claude-opus-5"
routing           = "defect"
+++

### UNT-106 - The world is alive behind the count-in, so nothing changes on GO

- Routing class: `defect` -> **claude-opus-5**, effort **high**. Bias 1. Finding the cause is the work, and a wrong guess costs a second pass. ([[RLG-092]])
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-31T15:36:03-04:00** - step() opened with a comment saying everything below it runs so the road, the traffic and the sky are alive behind the numbers, and then returned two lines under it. So the whole world was frozen for three seconds and resumed in one frame - which is the pop on GO the owner has reported four times. The worst of it is viewShift, the forward view's horizontal offset, assigned only inside step(): 0 during the count and -40.8 pixels after it, so the entire picture and the player's car moved sideways in one frame. The return is gone and a `held` flag replaces it, gating the car by name at each place that would move it, score it, hurt it or spend its clock.
