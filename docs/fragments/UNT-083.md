+++
id                = "UNT-083"
kind              = "unit"
status            = "closed"
cost              = 7990
closed            = "2026-08-30T20:48:54-04:00"
created           = "2026-08-30T20:43:43-04:00"
created_precision = "second"
source            = "owner report, 2026-08-30"
supersedes        = []
blocks            = []
caused_by         = ["RLG-088"]
see_also          = []
delegate          = false
effort            = "medium"
model             = "claude-sonnet-5"
ran_on            = "claude-opus-5"
routing           = "feature"
+++

### UNT-083 - The world is already there when the count starts, and the pips do not climb

- Routing class: `feature` -> **claude-sonnet-5**, effort **medium**. Bias 1. Implementation to a spec that already answers the design questions. ([[RLG-092]])
- Opened for: [[RLG-088]]
- Opened by `unit.py`. **The identity exists before the first edit** ([[RLG-097]]), so the snapshot has somewhere to go.
- **CLOSED 2026-08-30T20:48:54-04:00** - Two regressions from the countdown, both mine. THE WORLD CHANGED AT GO because two correct changes met badly: the reset clears the opening-place flag so a run picks its own place, and the count-in returns from step() before stepBiome runs - so the place was chosen on the first frame AFTER the count, in front of the player. Measured FOREST for three seconds then OCEAN at GO. The pick is its own function and the reset calls it, so the world you look at is the world you drive into. AND THE PIPS NO LONGER CLIMB: the owner is right that a countdown should not rise, and the note on the run-out tick in this same file already said so - a rising pitch reads as a fanfare. One 392 Hz note, struck harder each second, which leaves GO as the only different note. The check samples the place across the count and past it and compares the first against every later one; leaving the pick to the first frame fails it with the exact transition the owner described.
